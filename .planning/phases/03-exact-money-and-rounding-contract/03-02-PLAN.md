---
phase: 03-exact-money-and-rounding-contract
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - supabase/migrations/20260902000001_exact_financial_primitives.sql
  - supabase/tests/database/60_exact_financial_primitives.sql
autonomous: true
requirements: [CALC-01, CALC-03]
must_haves:
  truths:
    - "D-01: D-02: PostgreSQL stores USD minor units and canonical rate components in checked signed bigint fields while using wider exact numeric only for intermediates."
    - "D-05: D-08: D-13: Currency, rate, and rounding policy identities are immutable, named, versioned, bounded, and cannot be browser-mutated."
    - "D-11: D-12: D-14: Database rounding matches the signed half-away runtime contract, rounds only at an explicit boundary, and fails closed on invalid policy, zero denominator, or overflow."
    - "D-19: D-21: D-23: PostgreSQL boundary helpers require JSON strings, emit strings, and have independent live golden/property/ACL proof."
  artifacts:
    - path: supabase/migrations/20260902000001_exact_financial_primitives.sql
      provides: "Immutable financial policy catalogs and hardened exact PostgreSQL helper kernel"
      contains: "half-away-from-zero-v1"
    - path: supabase/tests/database/60_exact_financial_primitives.sql
      provides: "Catalog, ACL, parser, rate, signed-rounding, range, and property pgTAP proof"
  key_links:
    - from: public policy catalogs
      to: private exact helpers
      via: "explicit version/currency/exponent validation"
    - from: private exact helpers
      to: public boundary functions
      via: "string-only JSON input/output and explicit grants"
---

<objective>
Install the matching exact financial contract inside PostgreSQL.

Purpose: Client exactness is advisory until the database independently enforces
currency, range, rate, policy, serialization, and rounding invariants.
Output: Immutable catalogs, private hardened helpers, narrow public boundary
functions, and live pgTAP contracts.
</objective>

<decision_coverage>
- D-01, D-02: Checked minor-unit persistence and wider exact intermediates.
- D-05, D-08, D-13: Reduced components and immutable versioned policies.
- D-11, D-12, D-14: Explicit half-away boundary behavior and stable failures.
- D-19, D-21, D-23: String-token JSON and live PostgreSQL proof.
</decision_coverage>

<execution_context>
@.planning/phases/03-exact-money-and-rounding-contract/03-CONTEXT.md
@.planning/phases/03-exact-money-and-rounding-contract/03-RESEARCH.md
@.planning/phases/03-exact-money-and-rounding-contract/03-PATTERNS.md
@.planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
@.planning/phases/03-exact-money-and-rounding-contract/03-01-SUMMARY.md
@supabase/migrations/20260901000001_billing_tenant_roles.sql
@supabase/migrations/20260901000003_billing_automation_grants.sql
@supabase/tests/database/30_billing_tenancy.sql
@supabase/tests/database/35_billing_automation.sql
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install immutable policies and hardened exact helpers</name>
  <files>supabase/migrations/20260902000001_exact_financial_primitives.sql, supabase/tests/database/60_exact_financial_primitives.sql</files>
  <read_first>
    - Plan 01 summary and exact fixture semantics
    - Phase 2 private-helper, `search_path`, ACL, and immutable catalog patterns
    - Current `00_schema_contracts.sql` and `30_billing_tenancy.sql` catalog assertions
  </read_first>
  <behavior>
    - Catalogs contain exactly `usd-v1`, `ordinary-percentage-v1`, and `half-away-from-zero-v1` with USD exponent 2 and nine fractional percentage digits.
    - Global server-owned catalog rows use force RLS, cannot be inserted, updated, or deleted through browser roles, and are protected from accidental mutation; tenant `sales_id` ownership is explicitly inapplicable to these reference rows.
    - Helpers validate string JSON token type before casting, normalize exact ratios, keep denominator positive, and check signed-`bigint` persistence bounds.
    - Private/definer functions use empty search path, full qualification, revoked PUBLIC execution, and the minimum required public wrapper grants.
  </behavior>
  <action>Write pgTAP catalog/ACL/parser failures before the migration. Create normalized immutable currency/rate/rounding policy catalogs and private exact helpers in one transaction. Treat catalogs as global server-owned reference data rather than tenant rows: enable/force RLS, grant no browser DML, expose only the intended safe read/helper boundary, and document why the account `sales_id` trigger pattern does not apply. Use PostgreSQL `numeric` only as a wider exact intermediate; store canonical components in checked `bigint`. Implement strict JSON string extraction, canonical integer/rate normalization, GCD reduction, and stable safe error codes without echoing raw input. Avoid custom PostgreSQL `money`, float types, mutable defaults, and generic execute grants. [BLOCKING] After the migration exists and before accepting the task, run the isolated loopback `make test-financial-schema-push`; never use linked or dashboard production mode.</action>
  <acceptance_criteria>
    - Catalog constraints, force-RLS state, immutability, exact seeded rows, function search paths, ownership, and ACLs are asserted live.
    - JSON numeric tokens, malformed strings, unsupported currency/policy, nonpositive denominators, and out-of-range persistence values fail closed.
    - `make test-financial-schema-push` installs the ordered chain in the disposable target and exits 0.
  </acceptance_criteria>
  <verify><automated>make test-financial-schema-push</automated></verify>
  <done>The exact policy and validation kernel is installed without widening browser or public authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove PostgreSQL rate and signed rounding parity independently</name>
  <files>supabase/tests/database/60_exact_financial_primitives.sql</files>
  <read_first>
    - Task 1 migration functions and exact signatures
    - `exactFinancialFixtures.ts` values, translated independently rather than imported
    - Existing pgTAP transaction/role/reset conventions in `35_billing_automation.sql`
  </read_first>
  <behavior>
    - Percentage inputs reduce to the same values as TypeScript, including `12.500% -> 1/8` and `8.875% -> 71/800`.
    - Signed tie and non-tie cases match `half-away-from-zero-v1`; exact division does not round.
    - Repeated bounded generated cases prove reduction idempotence, sign symmetry, equivalent-ratio equality, and monotonic rounding.
    - Full signed endpoints succeed exactly and one-step overflow fails without a persisted or audited effect.
  </behavior>
  <action>Complete the pgTAP file with an exact declared plan, independent golden vectors, bounded generated cases, stable error assertions, role/ACL checks, and transaction rollback. Include positive and negative endpoints, minimum-value absolute intermediate behavior, denominator zero, policy mismatch, exponent mismatch, unsupported currency, rate bounds/precision, and JSON token-type rejection. Do not satisfy behavior with migration source-string checks.</action>
  <acceptance_criteria>
    - Every golden TypeScript semantic has an independent PostgreSQL assertion.
    - Generated cases are deterministic, bounded, exact, and leave no production test objects or rows after rollback.
    - The database financial lane executes the new pgTAP file and exits 0.
  </acceptance_criteria>
  <verify><automated>make test-financial-database-sql</automated></verify>
  <done>The database independently proves the same exact rate and named rounding behavior as the runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Public/RPC JSON → PostgreSQL exact value | A numeric token or unsafe string could bypass browser validation. |
| Policy catalog → helper behavior | Mutable or substituted policy rows could alter later replay. |
| Privileged helper → database objects | Search-path or execute-grant mistakes could expand authority. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | JSON boundary | mitigate | Token-type tests before cast; canonical string-only outputs. |
| T-03-03 | Tampering / Repudiation | policy/rounding | mitigate | Immutable version catalog plus explicit helper inputs and fixtures. |
| T-03-05 | Elevation | helper ACL/search path | mitigate | Private schema, empty search path, qualification, PUBLIC revoke, live catalog assertions. |
| T-03-SC | Tampering | migration supply chain | mitigate | Existing ordered/full-SHA protected lane and no new dependencies. |
</threat_model>

<verification>
- `make test-financial-schema-push`
- `make test-financial-database-sql`
- `git diff --check`
</verification>

<success_criteria>
- PostgreSQL enforces exactly the same money/rate/rounding vocabulary as TypeScript.
- Policy and helper identities are immutable, explicit, and least privilege.
- Live tests reject unsafe tokens and prove signed boundary behavior.
</success_criteria>

<output>Create `.planning/phases/03-exact-money-and-rounding-contract/03-02-SUMMARY.md` when done.</output>
