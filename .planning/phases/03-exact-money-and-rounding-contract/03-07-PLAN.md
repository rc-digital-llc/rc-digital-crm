---
phase: 03-exact-money-and-rounding-contract
plan: "07"
type: execute
wave: 7
depends_on: ["03-06"]
files_modified:
  - makefile
  - .github/release/financial-paths.json
  - tests/release/exact-money-release-static.test.ts
  - .planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
autonomous: true
requirements: [CALC-01, CALC-03]
must_haves:
  truths:
    - "D-01 through D-23: Every money, rate, rounding, migration, caller-bound RPC, evidence wrapper, provider, preview, and anti-floating-point contract is represented in an existing non-optional protected lane."
    - "Plans 01–06 coupled each new money-bearing path/test in its introducing plan; this plan audits completed membership and is never the first protection point."
    - "Direct authenticated invoice table/sequence access stays revoked; no security-invoker read view or numeric automation/evidence helper survives the final static/live matrix."
    - "Full signed-bigint range, 64/14 pre-parse limits, exact line items, canonical automation/effect fingerprints, non-negative automation, bounded D-10 audit, historical immutability, and Phase 3-only scope remain green together."
  artifacts:
    - path: tests/release/exact-money-release-static.test.ts
      provides: "Final closed audit of rolling path/lane/RPC/anti-float/history coupling"
    - path: .planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
      provides: "Actual task-to-proof evidence and explicit post-merge hosted boundary"
    - path: makefile
      provides: "Existing stable financial targets execute every Phase 3 contract"
  key_links:
    - from: all Phase 3 implementation/test paths
      to: existing six financial workflow identities
      via: "path classification and explicit Make target lists established in Plans 01–06"
    - from: merged build receipt
      to: protected schema promotion
      via: "content-addressed predecessor evidence and owner-gated production-release environment"
---

<objective>
Close Phase 3 through the inherited blocking financial release contract.

Purpose: Exact behavior is not releasable if any path can skip its lane or if
local/merge proof is confused with hosted schema proof.
Output: Final rolling-coupling audit, integrated validation evidence, and an
explicit protected post-merge promotion boundary.
</objective>

<decision_coverage>
- D-01 through D-23: Plans 01–06 implement and protect every locked decision; Plan 07 verifies the complete matrix.
- D-16 through D-18: Upgrade/schema/history evidence remains blocking and additive.
- D-20, D-22, D-23: Runtime/provider/live/property tests execute in the inherited protected lanes.
</decision_coverage>

<execution_context>
@.planning/phases/03-exact-money-and-rounding-contract/03-CONTEXT.md
@.planning/phases/03-exact-money-and-rounding-contract/03-REVIEWS.md
@.planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
@.planning/phases/03-exact-money-and-rounding-contract/03-SOURCE-AUDIT.md
@.planning/phases/03-exact-money-and-rounding-contract/03-06-SUMMARY.md
@makefile
@.github/workflows/financial-release-gate.yml
@.github/workflows/release-build.yml
@.github/workflows/release-promote.yml
@.github/release/financial-paths.json
@tests/release/billing-security-static.test.ts
@tests/release/exact-money-release-static.test.ts
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit rolling same-plan coupling and Cycle 2 security closure</name>
  <files>makefile, .github/release/financial-paths.json, tests/release/exact-money-release-static.test.ts</files>
  <read_first>
    - Plans 01–06 summaries and their exact source/test/target ownership
    - Current Make financial lists, path classifier, and all six financial workflow jobs/triggers
    - Cycle 2 C2-H1/C2-H2/C2-W1/C2-W2 closure map in `03-REVIEWS.md`
    - `release-promote.yml` protected schema stage and predecessor-receipt contract
  </read_first>
  <behavior>
    - Final static proof enumerates the Wave 1 unit/static, Wave 2 primitive pgTAP, Wave 3 upgrade verifier, Wave 4 database/evidence/replay cutover, Wave 5 live Supabase, and Wave 6 FakeRest/preview contracts.
    - Each source/test is classified and its applicable focused test is in the correct existing fast/database SQL/database HTTP/functions/upgrade/replay target from its introducing plan.
    - Authenticated base invoice table/sequence grants, invoice security-invoker read views, unbounded/dynamic read RPCs, client tenant authority, numeric automation signatures, the old evidence numeric dependency, float authority, and modified accepted migrations fail static closure.
    - Existing six financial identities, unconditional merge-group behavior, isolation, timeouts, pinned actions/CLI, cleanup, no assertion retry, read-only PR permissions, and build-without-production-mutation stay unchanged.
  </behavior>
  <action>Run the rolling static matrix first and compare all Phase 3 paths/tests to their introducing plan and permanent target. Correct only discovered coupling omissions; never use this plan as the first planned membership for an earlier path. Verify explicit test membership for exact money, tests 60/65, upgrade Vitest/runner, inherited tests 35/40, replay/evidence, billing tenancy/live boundary, provider parity, invoice preview, and the static test itself. Add closed source assertions for the two caller-bound SECURITY DEFINER read RPCs, empty search paths, full qualification, locked owner/ACL, capability predicates, bounded allowlists/no dynamic SQL, direct-table revoke, exact evidence helper/wrapper, canonical zero/effect fingerprint, and byte-identical accepted migrations. Preserve the six existing protected identities and owner-gated production promotion; do not add an optional seventh check, retry assertions, or local/PR production credentials.</action>
  <acceptance_criteria>
    - Every Phase 3 path/test is classified and explicitly executed by the correct existing protected lane, with introducing-plan summaries proving same-plan coupling.
    - Static mutation/removal fixtures catch every Cycle 2 security regression and historical migration change.
    - Required job names/count and unconditional merge-group behavior are unchanged.
    - Only `release-promote.yml` can run linked schema push, with exact predecessor evidence and protected environment approval.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/exact-money-release-static.test.ts</automated></verify>
  <done>Rolling coupling and C2-H1/H2 closure are complete without making Plan 07 the first gate for any earlier path.</done>
</task>

<task type="auto">
  <name>Task 2: Run integrated proof and record the hosted release boundary</name>
  <files>.planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md</files>
  <read_first>
    - Complete seven-plan per-task map and owner-controlled section in `03-VALIDATION.md`
    - Plans 01–06 summaries, command outputs, fingerprints, and exact implementation head
    - Phase 1 build/promotion receipt contract and any applicable rendered-surface rule
  </read_first>
  <behavior>
    - One exact implementation head passes the full financial gate, typecheck, lint, and build without assertion retry.
    - Validation records actual test counts/fingerprints and distinguishes pending Wave 0, local, merge-group, build, schema-promotion, and production readback evidence.
    - No hosted/live or rendered claim is made from local tests, merge, or build alone.
  </behavior>
  <action>Run the full phase command once at the integrated head. Record actual test counts, migration/upgrade fingerprints, command outcomes, exact head, and residual manual/hosted boundaries in VALIDATION.md; mark Wave 0 complete only after every named file exists and executes. If rendered invoice UI changed, require the full source/immutable-preview/production surface loop before readiness/live claims; otherwise record why no rendered receipt applies. After exact-head approval and merge queue, require default-branch release build plus separately owner-approved `release-promote` schema stage and provider post-state readback before claiming the exact contract live.</action>
  <acceptance_criteria>
    - `make financial-gate`, typecheck, lint, and build exit 0 at one exact integrated head.
    - VALIDATION.md contains no pending evidence disguised as green and includes all seven plans/tasks.
    - Hosted schema and rendered UI claims remain gated by their distinct production evidence.
  </acceptance_criteria>
  <verify><automated>make financial-gate &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated></verify>
  <done>Phase 3 is locally and CI-ready proven, with hosted promotion correctly gated until authorized release/readback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Changed path → CI classification | Missing paths could skip exact/security tests. |
| Integrated proof → merge readiness | Results from another head could be substituted. |
| Merge/build → hosted schema | Successful code integration could be misreported as production deployment. |
| Promotion evidence → production mutation | Stale predecessor artifacts could authorize the wrong schema. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering / Repudiation | gate/path coupling | mitigate | Closed seven-plan static matrix and exact-head evidence. |
| T-03-10 | Elevation / Information Disclosure | invoice reads | mitigate | RPC-only/static-live closure; base-table revoke remains blocking. |
| T-03-14 | Tampering | evidence helper | mitigate | Exact canonical-zero dependency and conflict-atomic live tests. |
| T-03-06 | Tampering | upgrade exception path | mitigate | Blocking clean/upgrade/schema lanes and immutable registry/history checks. |
| T-03-08 | Elevation | production schema path | mitigate | Build cannot mutate; protected promotion re-verifies predecessor before push. |
| T-03-SC | Tampering | workflow supply chain | mitigate | Preserve full-SHA actions, pinned CLI, locked install, and secret gates. |
</threat_model>

<verification>
- `npm test -- --run tests/release/exact-money-release-static.test.ts`
- `make financial-gate && npm run typecheck && npm run lint && npm run build`
- Exact-head owner review and fresh merge-group checks occur after execution.
- Default-branch build and owner-approved protected schema promotion/readback occur after authorized merge before any live claim.
</verification>

<success_criteria>
- Every CALC-01/CALC-03 and D-01–D-23 path is blocking and exact-head coupled.
- Plans 01–06 introduced classifier/Make/static protection in the same plan as each money-bearing path/test.
- C2-H1/H2/W1/W2 are protected against regression.
- Production mutation remains separated, owner-gated, content-addressed, and read back.
</success_criteria>

<output>Create `.planning/phases/03-exact-money-and-rounding-contract/03-07-SUMMARY.md` when done.</output>
