---
phase: 03-exact-money-and-rounding-contract
plan: "05"
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - makefile
  - .github/release/financial-paths.json
  - tests/release/exact-money-release-static.test.ts
  - .planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
autonomous: true
requirements: [CALC-01, CALC-03]
must_haves:
  truths:
    - "D-01: D-05: D-11: D-19: Every money, rate, rounding, migration, live boundary, provider, and anti-floating-point contract runs in an existing non-optional protected lane."
    - "D-16: D-17: D-18: Clean install, exact legacy upgrade, isolated schema push, safe exception behavior, and immutable fingerprints block release on failure."
    - "D-20: D-22: D-23: Runtime, PostgreSQL, Supabase, FakeRest, property, JSON, overflow, malformed, and signed-tie tests are all included in the integrated exact-head proof."
    - "All D-01 through D-23 remain represented by the combined five-plan contract and are not weakened by release classification or evidence handling."
  artifacts:
    - path: makefile
      provides: "Existing stable financial targets execute every Phase 3 contract"
    - path: tests/release/exact-money-release-static.test.ts
      provides: "Closed path/lane/anti-float/legacy-RPC/schema-promotion coupling assertions"
    - path: .planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
      provides: "Final task-to-proof evidence and explicit post-merge hosted boundary"
  key_links:
    - from: exact money source/schema/test paths
      to: existing financial workflow identities
      via: "path classification and explicit Make target lists"
    - from: merged release build receipt
      to: protected schema promotion
      via: "content-addressed predecessor evidence and owner-gated production-release environment"
---

<objective>
Close Phase 3 through the inherited blocking financial and release contract.

Purpose: Exact behavior is not assured if a changed file or test can skip the
required lanes, or if merge/build evidence is mistaken for hosted schema proof.
Output: Closed release coupling, static policy tests, full integrated validation,
and an explicit protected post-merge schema-promotion boundary.
</objective>

<decision_coverage>
- D-01 through D-23: All implementation decisions are covered across Plans 01–04 and made non-optional here.
- D-16 through D-18: Upgrade/schema/fingerprint lanes remain blocking.
- D-20, D-22, D-23: Runtime/provider/live/property suites are coupled to protected checks.
</decision_coverage>

<execution_context>
@.planning/phases/03-exact-money-and-rounding-contract/03-CONTEXT.md
@.planning/phases/03-exact-money-and-rounding-contract/03-RESEARCH.md
@.planning/phases/03-exact-money-and-rounding-contract/03-PATTERNS.md
@.planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
@.planning/phases/03-exact-money-and-rounding-contract/03-04-SUMMARY.md
@makefile
@.github/workflows/financial-release-gate.yml
@.github/workflows/release-build.yml
@.github/workflows/release-promote.yml
@.github/release/financial-paths.json
@tests/release/billing-security-static.test.ts
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Couple every exact contract into existing blocking lane identities</name>
  <files>makefile, .github/release/financial-paths.json, tests/release/exact-money-release-static.test.ts</files>
  <read_first>
    - Current Make financial test lists/targets and exact workflow job names/triggers
    - Financial path classifier config and Phase 1/2 release-policy static tests
    - Every Phase 3 implementation/test path from `03-VALIDATION.md`
    - `release-promote.yml` schema stage and predecessor-receipt contract
  </read_first>
  <behavior>
    - New pure unit/static tests run in an existing fast required lane; provider parity/live HTTP, pgTAP, and upgrade tests run in their existing correct isolated lanes.
    - Every exact financial source, migration, provider, fixture, and test path classifies as financial and all merge-group lanes remain unconditional.
    - Static checks reject optional/renamed/skipped/retried exact tests, numeric RPC authority, raw bigint JSON assumptions, and direct production mutation from PR/build jobs.
    - Production schema push remains only in the existing `production-release` protected promotion using exact predecessor evidence.
  </behavior>
  <action>Add failing coupling and anti-pattern tests first, then extend existing Make file lists and narrow financial path globs. Preserve exactly the current six required financial identities, merge-group trigger semantics, pinned actions/Supabase CLI, timeouts, isolated loopback runner, cleanup, zero assertion retry, read-only PR permissions, and build-without-production-mutation rule. Do not introduce an optional seventh check or a local/PR production credential. The static test may inspect source coupling but cannot replace live exact behavior.</action>
  <acceptance_criteria>
    - Every Phase 3 file/test is classified and explicitly executed by the correct existing protected lane.
    - Required job names/count and unconditional merge-group behavior are unchanged.
    - Tests prove only `release-promote.yml` can run linked schema push and that it remains protected by predecessor evidence plus production environment approval.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/exact-money-release-static.test.ts</automated></verify>
  <done>No exact-money change or acceptance test can merge through an unclassified, optional, or production-bypassing path.</done>
</task>

<task type="auto">
  <name>Task 2: Run integrated proof and record the release boundary</name>
  <files>.planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md</files>
  <read_first>
    - Complete per-task map and owner-controlled section in `03-VALIDATION.md`
    - Plans 01–04 summaries and exact command outputs/commit identities
    - Existing build/promotion receipt contract from Phase 1 verification
  </read_first>
  <action>Run the full phase command once without assertion retries at the integrated implementation head. Record actual test counts, migration/upgrade fingerprints, command outcomes, exact head, and any residual boundary in VALIDATION.md; set Wave 0 complete only after every named file exists and executes. No rendered receipt is required unless the implementation changed rendered invoice UI—in that case apply the full source/immutable-preview/production surface loop before readiness/live claims. Do not claim hosted/live from local proof: record that after the implementation PR is exact-head approved, merge-queued, and merged, the default-branch release build must succeed and its content-addressed evidence must pass the separately owner-approved `release-promote` schema stage with provider post-state readback.</action>
  <acceptance_criteria>
    - `make financial-gate`, typecheck, lint, and build exit 0 at one exact integrated head.
    - VALIDATION.md contains no pending implementation/Wave 0 evidence disguised as green and names the exact post-merge protected schema boundary.
    - No UI live claim is made without applicable staged surface receipts; no production schema live claim is made before protected promotion/readback.
  </acceptance_criteria>
  <verify><automated>make financial-gate &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated></verify>
  <done>Phase 3 implementation is locally and CI-ready proven, with hosted promotion still correctly gated until exact-head merge and owner-approved schema release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Changed path → CI classification | Missing paths could skip live exact tests. |
| Local/integrated proof → merge readiness | Results from another head could be substituted. |
| Merge/build → hosted schema | Successful code integration could be misreported as production deployment. |
| Promotion evidence → production mutation | Stale or wrong predecessor artifacts could authorize the wrong schema. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering / Repudiation | gate/path coupling | mitigate | Closed static matrix, exact existing identities, exact-head evidence. |
| T-03-06 | Tampering | upgrade exception path | mitigate | Blocking clean/upgrade/schema lanes and immutable registry checks. |
| T-03-01 | Tampering | skipped live boundary | mitigate | Explicit HTTP/pgTAP inclusion in required lanes. |
| T-03-08 | Elevation | production schema path | mitigate | Build cannot mutate; protected promotion re-verifies predecessor immediately before push. |
| T-03-SC | Tampering | workflow supply chain | mitigate | Preserve full-SHA actions, pinned CLI, locked install, and secret gates. |
</threat_model>

<verification>
- `npm test -- --run tests/release/exact-money-release-static.test.ts`
- `make financial-gate && npm run typecheck && npm run lint && npm run build`
- Exact-head owner review and fresh merge-group checks occur after execution, not as a local substitute.
- Default-branch build and owner-approved protected schema promotion/readback occur after authorized merge before any live claim.
</verification>

<success_criteria>
- Every CALC-01/CALC-03 acceptance path is blocking and exact-head coupled.
- Full local/integrated proof is recorded without assertion retry or evidence overclaim.
- Production mutation remains separated, owner-gated, content-addressed, and read back.
</success_criteria>

<output>Create `.planning/phases/03-exact-money-and-rounding-contract/03-05-SUMMARY.md` when done.</output>
