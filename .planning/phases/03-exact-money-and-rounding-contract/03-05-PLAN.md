---
phase: 03-exact-money-and-rounding-contract
plan: "05"
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - src/components/atomic-crm/types.ts
  - src/components/atomic-crm/providers/types.ts
  - src/components/atomic-crm/providers/supabase/dataProvider.ts
  - tests/release/exact-money-boundaries.test.ts
  - tests/release/billing-tenancy.test.ts
  - makefile
  - .github/release/financial-paths.json
  - tests/release/exact-money-release-static.test.ts
autonomous: true
requirements: [CALC-01, CALC-03]
must_haves:
  truths:
    - "D-03: D-04: D-19: D-21: Supabase invoice reads/writes use typed canonical string money/rate objects and stable safe errors; numeric or malformed financial tokens never regain authority."
    - "React Admin `getList` and `getOne` translate only into the closed, bounded caller-bound read RPC contract; no generic invoice-table CRUD path survives."
    - "Live Auth/PostgREST proof covers same-tenant list/get success, cross-tenant and forged-scope denial, direct table/sequence denial, unsafe request denial, full signed-bigint strings, and exact `tax_rate` compatibility: `8.875% -> 8.875000000` and `12.500% -> 1/8` with submitted evidence preserved."
    - "The exact boundary, inherited tenancy caller, shared types, Supabase adapter, and focused tests enter their protected classifier/HTTP target in this plan."
  artifacts:
    - path: src/components/atomic-crm/providers/supabase/dataProvider.ts
      provides: "Validated compound invoice list/get/save methods over exact RPCs only"
    - path: tests/release/exact-money-boundaries.test.ts
      provides: "Live caller-bound RPC, token, pagination, range, and unchanged-effect proof"
    - path: tests/release/billing-tenancy.test.ts
      provides: "Inherited tenant matrix migrated away from direct invoice-table reads"
  key_links:
    - from: React Admin getList/getOne
      to: public.read_billing_invoices_exact(jsonb)
      via: "explicit request translation and response validation"
    - from: exact-money-boundaries.test.ts
      to: local Auth/PostgREST RPC endpoints
      via: "real JWTs and service-only postcondition reads"
    - from: Wave 5 source/tests
      to: FINANCIAL_DATABASE_HTTP_TESTS and financial path classifier
      via: "same-plan Make/static coupling"
---

<objective>
Expose and prove the exact invoice contract through the live Supabase boundary.

Purpose: Database hardening is incomplete until React Admin and real JWT clients
can read legitimate rows without restoring base-table privilege or unsafe JSON.
Output: Shared/provider types, exact Supabase compound methods, live boundary and
tenant tests, and immediate protected-lane coupling.
</objective>

<decision_coverage>
- D-03, D-04, D-19, D-21: Canonical typed string objects and strict request/response validation.
- D-06 through D-09: Exact embedded rate/evidence/policy fields cross the provider boundary unchanged.
- D-14 and D-23: Stable failure, full-range, malformed, and live HTTP proof.
- D-20 and D-22: Shared branded types define the production provider half of parity.
</decision_coverage>

<execution_context>
@.planning/phases/03-exact-money-and-rounding-contract/03-CONTEXT.md
@.planning/phases/03-exact-money-and-rounding-contract/03-RESEARCH.md
@.planning/phases/03-exact-money-and-rounding-contract/03-PATTERNS.md
@.planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
@.planning/phases/03-exact-money-and-rounding-contract/03-01-SUMMARY.md
@.planning/phases/03-exact-money-and-rounding-contract/03-04-SUMMARY.md
@src/components/atomic-crm/providers/supabase/dataProvider.ts
@src/components/atomic-crm/providers/types.ts
@tests/release/billing-tenancy.test.ts
@makefile
@.github/release/financial-paths.json
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace generic invoice access with validated Supabase RPC methods</name>
  <files>src/components/atomic-crm/types.ts, src/components/atomic-crm/providers/types.ts, src/components/atomic-crm/providers/supabase/dataProvider.ts</files>
  <read_first>
    - Plan 01 branded codec exports and Plan 04 exact read/write RPC request/response shapes
    - Existing React Admin `getList`/`getOne` parameter conventions and compound billing methods in the Supabase provider
    - Current shared invoice and billing types; identify every authoritative `number` field to replace
  </read_first>
  <behavior>
    - Shared invoice records use exact money/rate/line-item objects; authoritative integer components are strings and cannot be constructed with casts alone.
    - Invoice `getList` maps one-based React Admin page/perPage, sort/order, and allowlisted filters to `read_billing_invoices_exact`; `getOne` maps the ID to mode `get`; both validate the entire `{data,total}` or single-data response before branding.
    - Unknown filter/sort keys, page/perPage outside the database contract, browser-supplied organization/sales IDs, and incompatible operation keys fail locally with the same stable safe code before an RPC call.
    - Invoice create/update uses only `save_billing_invoice_exact`; generic `.from("invoices")` read/write and the numeric automation method are absent from the authoritative provider path.
    - The provider does not invent or transmit an invoice-save idempotency key/fingerprint; valid exact saves succeed, while invalid input is rejected with invoice/audit effects unchanged. Draft business idempotency remains Phase 5 `INV-01` scope.
    - Exact and compatibility read responses preserve `submitted_percentage` separately from the reduced ratio, validate `tax_rate` as a fixed-nine-decimal percentage string, and reject a narrowed, variable-scale, or financially unequal compatibility value.
  </behavior>
  <action>Add shared/provider contract failures first, then define exact invoice request/result types from Plan 01 branded values and Plan 04 wire fields. Implement explicit Supabase compound `listExactBillingInvoices`, `getExactBillingInvoice`, and `saveExactBillingInvoice` methods, or equivalent unambiguous names, and route React Admin invoice operations to them. Translate only the closed list/get keys and let an account filter narrow—not establish—authority. Validate every RPC response through the codec, including line items, canonical ratio, separately preserved `submitted_percentage`, policy identities, the fixed-nine-decimal compatibility `tax_rate`, and 64/14 limits; never use `as` as validation, `Number`, raw bigint JSON, dynamic query construction, or generic invoice-table CRUD. The save method must neither accept nor synthesize an invoice-save idempotency key/fingerprint; Phase 3 tests cover exact save success and invalid-input rejection with unchanged effects only. Preserve nonfinancial React Admin resources unchanged and do not add issuance, payment, Stripe, charging, invoice business idempotency, or Phase 4 formula behavior. This implements C2-H1's usable caller path and preserves the C3-H1 scope boundary.</action>
  <acceptance_criteria>
    - Shared/provider types contain no authoritative money/rate `number` fields or public constructor bypass.
    - React Admin list/get response totals and records are returned in expected shapes after complete codec validation.
    - Search finds no authoritative direct `.from("invoices")` read/write or numeric automation invocation in the Supabase provider.
  </acceptance_criteria>
  <verify><automated>npm run typecheck</automated></verify>
  <done>The production adapter can use the caller-bound RPCs without restoring direct table access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the live caller, tenant, token, and full-range boundary</name>
  <files>tests/release/exact-money-boundaries.test.ts, tests/release/billing-tenancy.test.ts</files>
  <read_first>
    - Existing live Supabase/Auth loopback guard, principal fixture aliases, and service-only postcondition helpers
    - Plan 04 read/write RPC exact names, closed keys, stable errors, ownership, and ACLs
    - Existing billing-tenancy invoice GET/POST/PATCH cases that currently target `public.invoices`
  </read_first>
  <behavior>
    - A legitimate administrator/operator/reviewer/auditor list/get returns only same-tenant invoices; cross-tenant IDs, forged organization/sales/filter scope, disabled callers, wrong capability, and unknown IDs expose no row.
    - Direct authenticated GET/POST/PATCH/DELETE to `/rest/v1/invoices` and sequence use are denied, while the exact save RPC succeeds only for invoice.create/update capability.
    - Both exact and compatibility read RPCs return `-9223372036854775808` and `9223372036854775807` money/rate components as JSON strings and fixed decimals `-92233720368547758.08`/`92233720368547758.07` as strings.
    - Compatibility reads return `8.875000000` for canonical `71/800`, and `12.500000000` for canonical `1/8` while the exact response still preserves submitted evidence `12.500%`; the live assertions reject `numeric(5,2)`, variable-scale output, ratio mismatch, or a new financial-version field.
    - Numeric financial tokens, 65-byte integer text, 15-byte percentage text, formatted/plus/exponent strings, unsupported currency/policy, one-step overflow, unknown keys, invalid list/get combinations, perPage 0/101, invalid page/sort/order/filter, and tenant identity keys fail safely.
    - Every denied write/unsafe request leaves invoice rows, sequence-visible effects, automation counters/executions, and financial audits unchanged as applicable.
  </behavior>
  <action>Write `exact-money-boundaries.test.ts` against the real loopback Auth/PostgREST/RPC process and migrate the invoice portion of `billing-tenancy.test.ts` to the exact RPCs. Use real role JWTs for same/cross-tenant matrices and service-only reads solely for setup/postconditions. Assert token types, exact strings, total/pagination semantics, stable non-reflective errors, locked function owner/ACL behavior, and zero-effect denials. Prove valid exact save success and invalid numeric/malformed/overlong/policy inputs leave invoice/audit effects unchanged, without sending or asserting an invoice save key/fingerprint. Keep explicit direct-table GET/POST/PATCH/DELETE denials as negative tests; do not grant the table back for setup. Exercise compatibility through `read_billing_invoices_legacy_compat`, never a view, and assert the live `8.875000000`/`12.500000000` strings, canonical `71/800`/`1/8` equality, and separately preserved `8.875%`/`12.500%` submitted evidence. Preserve all existing non-invoice Phase 2 tenancy assertions and inherited safe diagnostics.</action>
  <acceptance_criteria>
    - Same-tenant list/get and authorized save succeed over RPC, while cross-tenant and direct-table paths fail.
    - Full signed-bigint and compatibility values remain strings end-to-end; no collision/narrowing is accepted.
    - Live RPC proof rejects the inherited two-decimal tax-rate narrowing and proves both required fixed-nine-decimal examples without losing submitted percentage evidence or creating a financial version.
    - Every malformed/oversized/pagination/filter/authority denial has explicit unchanged postconditions.
    - The focused live lane exits 0 with no linked or remote mutation.
  </acceptance_criteria>
  <verify><automated>node scripts/release/run-supabase-lane.mjs run --lane database-contracts -- npm test -- --run tests/release/exact-money-boundaries.test.ts tests/release/billing-tenancy.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <done>Live HTTP proves caller-bound reads work and every direct or unsafe bypass fails without effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Protect the live Supabase boundary in Wave 5</name>
  <files>makefile, .github/release/financial-paths.json, tests/release/exact-money-release-static.test.ts</files>
  <read_first>
    - Completed Task 1 source paths and Task 2 live test paths
    - Existing `FINANCIAL_DATABASE_HTTP_TESTS`, classifier entries, and rolling Plan 01–04 assertions
    - Existing six required workflow identities and isolated database HTTP target
  </read_first>
  <behavior>
    - `exact-money-boundaries.test.ts` and inherited `billing-tenancy.test.ts` execute explicitly in `FINANCIAL_DATABASE_HTTP_TESTS`.
    - Shared types, provider types, the Supabase adapter, and both live tests classify as financial in this plan.
    - Static proof fails on RPC-to-view regression, direct invoice grant/provider CRUD, unsafe numeric output, omitted tests, or a seventh replacement lane.
  </behavior>
  <action>Add failing rolling assertions for the live boundary and its exact target memberships, then extend the existing HTTP list and narrow classifier patterns. Preserve inherited entries, target names, loopback isolation, workflow count, and no assertion retry. Run the permanent HTTP target after coupling; Plan 07 is only the final audit. This closes C2-W2's live-boundary half while preserving H3/M1.</action>
  <acceptance_criteria>
    - Removing either live test or any money-bearing source path from protected classification fails the static contract.
    - Static checks reject `security_invoker` invoice read views and positive authenticated invoice table/sequence grants.
    - The permanent database HTTP target runs both tests and exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/exact-money-release-static.test.ts &amp;&amp; make test-financial-database-http</automated></verify>
  <done>The live Supabase exact boundary is non-optional before FakeRest/preview work begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| React Admin params → read RPC | UI filters/pagination must not become tenant identity or unbounded work. |
| PostgREST JSON → branded application record | Unsafe numeric tokens or unchecked casts could reintroduce floating-point authority. |
| Authenticated HTTP → invoice base table | Direct REST could bypass string-token and definer-function validation. |
| New live paths/tests → protected CI | A correct boundary can still be skipped by explicit target lists. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Elevation / Information Disclosure | Supabase read boundary | mitigate | Caller-bound RPC, response validation, same/cross-tenant live matrix, direct-table denial. |
| T-03-13 | Tampering / DoS | React Admin request translation | mitigate | Closed keys, bounded pagination, allowlisted sorts/filters, no tenant identity. |
| T-03-01 | Tampering | JSON serialization | mitigate | String-token assertions, codec validation, full-range endpoints and collision cases. |
| T-03-12 | Tampering / DoS | compatibility RPC | mitigate | Caller-bound function, exact fixed-decimal money strings, and fixed-nine-decimal 0..100 tax-rate strings derived from the reduced canonical ratio. |
| T-03-09 | Tampering / Repudiation | Wave 5 coupling | mitigate | Same-plan classifier/Make/static updates and permanent target execution. |
| T-03-SC | Tampering | provider dependencies | mitigate | No package changes; existing locked clients and test runner. |
</threat_model>

<verification>
- `npm run typecheck`
- `node scripts/release/run-supabase-lane.mjs run --lane database-contracts -- npm test -- --run tests/release/exact-money-boundaries.test.ts tests/release/billing-tenancy.test.ts`
- `npm test -- --run tests/release/exact-money-release-static.test.ts`
- `make test-financial-database-http`
- `git diff --check`
</verification>

<success_criteria>
- React Admin and Supabase use only caller-bound exact RPCs for invoice list/get/save.
- Same-tenant reads work without base-table privileges; cross-tenant/direct/unsafe paths fail unchanged.
- Full-range financial data remains string-safe across real HTTP.
- All Wave 5 paths/tests are protected immediately.
</success_criteria>

<output>Create `.planning/phases/03-exact-money-and-rounding-contract/03-05-SUMMARY.md` when done.</output>
