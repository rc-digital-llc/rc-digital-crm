---
phase: 03-exact-money-and-rounding-contract
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - scripts/release/fingerprint-upgrade.mjs
  - tests/release/migration-upgrade.test.ts
  - makefile
  - tests/release/exact-money-release-static.test.ts
autonomous: true
requirements: [CALC-01, CALC-03]
must_haves:
  truths:
    - "D-18: Baseline 001, registry 002, and accepted Phase 2 migrations 20260901000002, 20260901000003, and 20260901000004 remain byte-identical while the runner learns only the narrow Phase 3 transform vocabulary."
    - "D-15: D-16: D-17: Upgrade proof can distinguish the authorized exact invoice, line-item, automation, evidence-wrapper, ACL, and compatibility transforms from unrelated CRM drift."
    - "D-02: D-19: Fingerprints preserve PostgreSQL text and canonical string wire values without JavaScript numeric coercion."
    - "The upgraded runner and its Vitest contract enter the protected upgrade/static contract in the plan that changes them."
  artifacts:
    - path: scripts/release/fingerprint-upgrade.mjs
      provides: "Closed Phase 3 transformation categories, invariants, and exact-text fingerprinting"
    - path: tests/release/migration-upgrade.test.ts
      provides: "Fail-closed registry, history-immutability, and numeric-coercion regression proof"
    - path: tests/release/exact-money-release-static.test.ts
      provides: "Same-plan protected membership and immutable-history assertions"
  key_links:
    - from: scripts/release/fingerprint-upgrade.mjs
      to: supabase/tests/upgrades/003-exact-money/expected-transformations.json
      via: "closed schema consumed after Plan 04 creates the deterministic registry"
    - from: tests/release/migration-upgrade.test.ts
      to: test-financial-migration-upgrade
      via: "explicit Make execution added before this plan completes"
---

<objective>
Prepare the immutable upgrade verifier for the exact billing cutover.

Purpose: The database migration must not be written until the accepted transform
vocabulary, prior-history hashes, and rejection behavior are executable.
Output: A closed upgrade-runner contract and protected tests that Plan 04 can
use to authorize only its deterministic migration output.
</objective>

<decision_coverage>
- D-02 and D-19: Upgrade fingerprints treat exact financial components as text.
- D-15 through D-18: Only classified billing transforms are authorized; prior history and unrelated CRM state remain immutable.
- D-23: Synthetic registry/reordering/coercion failures are executable before database cutover.
</decision_coverage>

<execution_context>
@.planning/phases/03-exact-money-and-rounding-contract/03-CONTEXT.md
@.planning/phases/03-exact-money-and-rounding-contract/03-RESEARCH.md
@.planning/phases/03-exact-money-and-rounding-contract/03-PATTERNS.md
@.planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
@.planning/phases/03-exact-money-and-rounding-contract/03-02-SUMMARY.md
@supabase/tests/baselines/001-pre-financial/fixtures.sql
@supabase/tests/upgrades/002-billing-tenancy/expected-transformations.json
@supabase/migrations/20260901000002_billing_invoice_boundary.sql
@supabase/migrations/20260901000003_billing_automation_grants.sql
@supabase/migrations/20260901000004_billing_evidence_security.sql
@scripts/release/fingerprint-upgrade.mjs
@tests/release/migration-upgrade.test.ts
@makefile
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the closed exact-upgrade vocabulary and immutable-history proof</name>
  <files>scripts/release/fingerprint-upgrade.mjs, tests/release/migration-upgrade.test.ts</files>
  <read_first>
    - Existing fingerprint queries, registry ordering, allowed invariant IDs, and Phase 2 registry tests
    - Baseline 001 and registry 002 manifests as immutable accepted inputs
    - Exact bytes and SHA-256 values of `20260901000002_billing_invoice_boundary.sql`, `20260901000003_billing_automation_grants.sql`, and `20260901000004_billing_evidence_security.sql`
    - Plan 04's required invoice, automation, evidence-wrapper, privilege, function, and compatibility outputs in `03-VALIDATION.md`
  </read_first>
  <behavior>
    - The runner accepts a future ordered 003 entry only for exact invoice values/line items, automation limits/executions/request fingerprints, the replaced evidence-finalization helper, read/write RPCs, ACLs, constraints, counts, IDs, and compatibility output.
    - Unknown, missing, overlapping, reordered, overbroad, stale, or mismatched transformations and invariants fail closed.
    - Baseline 001, registry 002, and all three named accepted Phase 2 migrations are hash-pinned and byte-identical.
    - Pre-values remain exact PostgreSQL text and post-values remain canonical string objects; neither path uses `Number`, `parseFloat`, or unsafe JSON numeric parsing.
  </behavior>
  <action>Write failing registry-schema, ordering, overlap, stale-hash, invariant, numeric-coercion, and immutable-prior-artifact tests before extending the runner. Add narrowly scoped query categories and semantic validators for the Plan 04 migration outputs, including revoked authenticated invoice table/sequence privileges, SECURITY DEFINER read/write RPC identities and ACLs, exact canonical line items, full-range compatibility strings, non-negative automation state, canonical request/effect fingerprints, and the replacement evidence-finalization helper. Pin the exact existing hashes of baseline 001, registry 002, and migrations `20260901000002`, `20260901000003`, and `20260901000004`; do not edit any accepted artifact. Define the 003 schema but do not create or guess final hashes here—Plan 04 writes the registry only after its migration output is deterministic. Preserve Phase 3-only scope and prove deal/project/analytics payloads unchanged. This closes C2-W1 while preserving H2/M2/M4/M5 and D-15 through D-18.</action>
  <acceptance_criteria>
    - Synthetic exact registries exercise every allowed category and every rejection family without requiring a remote database.
    - Earlier baselines, registries, and the three accepted Phase 2 migrations retain their pinned SHA-256 values.
    - Tests reject any floating-point conversion, omitted evidence-wrapper identity, privilege widening, or unrelated CRM transform.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/migration-upgrade.test.ts -t exact</automated></verify>
  <done>The upgrade runner can authorize only the exact Plan 04 cutover while preserving all accepted history byte-for-byte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect the upgraded runner before database cutover</name>
  <files>makefile, tests/release/exact-money-release-static.test.ts</files>
  <read_first>
    - Current `test-financial-migration-upgrade` recipe and the existing runner invocation
    - Plan 01 and Plan 02 rolling assertions in `exact-money-release-static.test.ts`
    - Completed Task 1 Vitest path and immutable-history expectations
  </read_first>
  <behavior>
    - `test-financial-migration-upgrade` executes both the live fingerprint runner and `migration-upgrade.test.ts`; neither is optional.
    - The rolling static contract fails if the runner test, any pinned historical migration, or the future 003 registry contract leaves the protected upgrade surface.
    - No new lane, assertion retry, remote database call, or production credential path is introduced.
  </behavior>
  <action>Add failing Plan 03 membership/immutability assertions to the rolling static test, then extend the existing upgrade target so the focused Vitest contract executes with the live runner. Preserve the current isolated loopback runner, target name, six required financial identities, and no-assertion-retry policy. Do not defer this protection to Plan 07 and do not create the 003 registry in this plan.</action>
  <acceptance_criteria>
    - Removing the upgrade Vitest or any immutable-history assertion deterministically fails the static contract.
    - The focused upgrade contract and protected upgrade target both exit 0 without linked or remote mutation.
    - All four files owned by this plan are classified by the existing financial path contract.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/exact-money-release-static.test.ts &amp;&amp; make test-financial-migration-upgrade</automated></verify>
  <done>The upgrade-verifier change is protected in Wave 3, before the dependent database cutover begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Transformation registry → accepted drift | An overbroad entry could legitimize unrelated or unsafe schema changes. |
| PostgreSQL text → JavaScript fingerprint | Numeric coercion could erase exact financial identity. |
| Accepted migration history → Phase 3 replacement | Editing history could counterfeit an additive cutover. |
| Upgraded runner → protected lane | A changed verifier could be present but never executed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering / Repudiation | upgrade registry | mitigate | Closed ordered categories, exact hashes/invariants, immutable prior artifacts. |
| T-03-02 | Tampering | exact fingerprints | mitigate | String-only PostgreSQL/JSON handling and numeric-coercion rejection tests. |
| T-03-09 | Tampering / Repudiation | upgrade coupling | mitigate | Same-plan Make/static membership and full protected target execution. |
| T-03-SC | Tampering | migration supply chain | mitigate | Existing ordered/full-SHA lane, no dependencies, no remote mutation. |
</threat_model>

<verification>
- `npm test -- --run tests/release/migration-upgrade.test.ts -t exact`
- `npm test -- --run tests/release/exact-money-release-static.test.ts`
- `make test-financial-migration-upgrade`
- `git diff --check`
</verification>

<success_criteria>
- The exact cutover has a closed executable transformation vocabulary before implementation.
- Accepted Phase 2 migrations, including billing evidence security, remain byte-identical.
- Upgrade-runner changes cannot skip the inherited protected lane.
</success_criteria>

<output>Create `.planning/phases/03-exact-money-and-rounding-contract/03-03-SUMMARY.md` when done.</output>
