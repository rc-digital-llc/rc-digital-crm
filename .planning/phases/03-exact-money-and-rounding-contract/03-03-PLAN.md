---
phase: 03-exact-money-and-rounding-contract
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - supabase/migrations/20260902000002_exact_billing_expand.sql
  - supabase/tests/database/65_exact_billing_conversion.sql
  - supabase/tests/upgrades/003-exact-money/expected-transformations.json
  - scripts/release/fingerprint-upgrade.mjs
  - tests/release/migration-upgrade.test.ts
autonomous: true
requirements: [CALC-01, CALC-03]
must_haves:
  truths:
    - "D-15: D-16: Invoices and automation amount authority convert additively to exact fields while unrelated CRM numerics remain unchanged and legacy fields become projections only."
    - "D-17: Any fractional-cent, malformed/ambiguous line item, currency contradiction, reconciliation failure, or range error aborts with a safe stable row/path exception; nothing is rounded, skipped, clamped, or guessed."
    - "D-18: Accepted baselines remain byte-identical and a numbered registry proves row counts, IDs, mappings, values, constraints, functions, and before/after fingerprints."
    - "D-01: D-02: D-05: D-06: D-07: D-08: D-11: D-12: D-13: D-14: D-19: D-21: D-23: Exact USD, rate, rounding, serialization, and live boundary rules govern every migrated field and command."
  artifacts:
    - path: supabase/migrations/20260902000002_exact_billing_expand.sql
      provides: "Invoice, line-item, and automation exact expand/backfill/cutover migration"
    - path: supabase/tests/upgrades/003-exact-money/expected-transformations.json
      provides: "Append-only exact conversion authorization and immutable fingerprints"
    - path: supabase/tests/database/65_exact_billing_conversion.sql
      provides: "Exact stored/projection/RPC/immutability and rejection contracts"
  key_links:
    - from: legacy invoice and automation numerics
      to: exact minor/rate/line-item fields
      via: "validate-all then deterministic backfill in one migration transaction"
    - from: fingerprint-upgrade.mjs
      to: 003-exact-money registry
      via: "ordered allowlisted pre/post categories and invariants"
---

<objective>
Convert every existing authoritative billing amount to the exact contract.

Purpose: Exact primitives do not satisfy CALC-01 while invoices, line items, and
automation authorization still accept decimal or weak JSON authority.
Output: One coordinated additive billing migration, immutable upgrade proof,
read-only compatibility projections, and live conversion contracts.
</objective>

<decision_coverage>
- D-01, D-02, D-05 through D-14, D-19, D-21, D-23: Applied to converted fields and RPC behavior.
- D-15: Inventory includes invoices plus automation grant/execution authority, excluding informational CRM numerics.
- D-16: Additive expand/backfill/cutover with no dual authority.
- D-17: Fail-closed conversion exception inventory and transaction abort.
- D-18: Immutable numbered fingerprint registry and later-only column removal.
</decision_coverage>

<execution_context>
@.planning/phases/03-exact-money-and-rounding-contract/03-CONTEXT.md
@.planning/phases/03-exact-money-and-rounding-contract/03-RESEARCH.md
@.planning/phases/03-exact-money-and-rounding-contract/03-PATTERNS.md
@.planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
@.planning/phases/03-exact-money-and-rounding-contract/03-02-SUMMARY.md
@supabase/migrations/20260305000004_add_invoices_table.sql
@supabase/migrations/20260901000002_billing_invoice_boundary.sql
@supabase/migrations/20260901000003_billing_automation_grants.sql
@supabase/tests/baselines/001-pre-financial/fixtures.sql
@supabase/tests/upgrades/002-billing-tenancy/expected-transformations.json
@scripts/release/fingerprint-upgrade.mjs
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend immutable upgrade proof for exact billing transforms</name>
  <files>scripts/release/fingerprint-upgrade.mjs, tests/release/migration-upgrade.test.ts</files>
  <read_first>
    - Existing fingerprint queries, registry validation, allowed invariants, and Phase 2 registry tests
    - Baseline 001 manifest/fingerprints/fixtures as immutable inputs
    - `03-RESEARCH.md` conversion inventory and expand-contract sequence
  </read_first>
  <behavior>
    - Baseline 001 and registry 002 remain byte-identical.
    - The registry schema accepts only a future 003 entry naming the new migration and exact categories for invoice values/line items, automation limits/effects, constraints/functions, counts, IDs, and projections.
    - Unknown, missing, overlapping, reordered, overbroad, stale, or mismatched transformations and semantic invariants fail closed.
    - Pre-values use exact PostgreSQL text; post-values use canonical string wire objects and never JavaScript numeric parsing.
  </behavior>
  <action>Add failing registry-schema, ordering, overlap, stale-hash, invariant, numeric-coercion, and immutable-prior-artifact tests before extending the runner. Add narrowly scoped fingerprint queries and semantic validators for exact invoices, line items, automation grants/executions, compatibility projections, constraints, function signatures/ACLs, and unchanged informational CRM payloads. Use strings from `psql`/JSON and never convert money/rate components to `number`. This task defines and tests the 003 schema but deliberately does not create the final registry; Task 2 creates it only after the migration output is deterministic.</action>
  <acceptance_criteria>
    - Earlier accepted artifacts have unchanged SHA-256 hashes.
    - The runner rejects every synthetic unregistered or overbroad transformation and validates unchanged deal/project/analytics payloads.
    - Focused upgrade tests pass without starting or mutating a remote database.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/migration-upgrade.test.ts -t exact</automated></verify>
  <done>The upgrade lane can authorize only the intended exact-money transform without weakening prior evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand, validate, backfill, and cut over exact billing authority</name>
  <files>supabase/migrations/20260902000002_exact_billing_expand.sql, supabase/tests/database/65_exact_billing_conversion.sql, supabase/tests/upgrades/003-exact-money/expected-transformations.json</files>
  <read_first>
    - Primitive migration/functions from Plan 02
    - Original invoice schema/trigger, Phase 2 invoice boundary migration, and baseline invoice fixtures
    - Automation grant/execution tables, consumption helper, public RPC, and `35_billing_automation.sql`
    - Task 1 registry/fingerprint contract
  </read_first>
  <behavior>
    - Invoice amount/tax/total convert only when multiplying by 100 is integral/in-range and USD is unambiguous; rate converts to a reduced embedded ratio/policy.
    - Each nonempty line item becomes exact JSON with reduced quantity ratio and typed USD unit/extended money, and reconciles with authoritative invoice values; malformed or ambiguous tokens identify row/path and abort.
    - Automation max/consumed/execution amounts convert to exact minor units; command input becomes typed money JSON with string minor units and the old numeric signature is no longer executable.
    - Exact fields alone drive totals, limits, effects, and new writes; retained decimal/legacy JSON fields are derived read-only compatibility projections.
    - A formatting-only percentage change appends presentation-history audit evidence but leaves the canonical ratio, policy identity, and financial version unchanged.
  </behavior>
  <action>Write pgTAP conversion/projection/rejection/immutability cases first. Implement an additive transaction: add nullable exact fields, build a safe local exception set, validate all rows before mutation, backfill deterministically through Plan 02 helpers, assert reconciliation/counts/IDs, add NOT NULL/check constraints, replace the invoice trigger and automation consumption/public RPC with exact-authority versions, and prevent client writes to legacy projections. Preserve Phase 2 tenant RLS, locks, idempotency, audits, append-only executions, timestamps, and provider-safe errors. Record formatting-only percentage changes as presentation-history audit without changing the canonical ratio/policy or creating a financial version. Do not alter project/deal/analytics numerics or drop legacy columns. Once outputs are stable, write the final numbered 003 registry hashes/invariants. [BLOCKING] Run the isolated upgrade proof and then isolated schema-push proof after schema changes and before database verification; never use a linked/remote push.</action>
  <acceptance_criteria>
    - Clean and baseline upgrade paths produce identical canonical exact values and registry 003 hashes.
    - Synthetic fractional-cent, malformed/ambiguous line item, mismatch, unsupported currency, numeric JSON, and overflow fixtures abort before any partial effect.
    - Compatibility decimals exactly derive from minor units and cannot contradict them through browser/RPC writes; formatting-only rate edits audit presentation history without financial drift.
    - `make test-financial-migration-upgrade`, `make test-financial-schema-push`, and the conversion pgTAP file all exit 0.
  </acceptance_criteria>
  <verify><automated>make test-financial-migration-upgrade &amp;&amp; make test-financial-schema-push &amp;&amp; make test-financial-database-sql</automated></verify>
  <done>Every current billing amount authority is exact, migrated fail-closed, and proven without destructive rewrite or dual authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Legacy decimals/JSON → exact authority | Ambiguity or fractional values could be guessed or rounded. |
| Transformation registry → accepted drift | An overbroad entry could legitimize unrelated changes. |
| Legacy/exact write surface → stored facts | Contradictory dual writes could choose authority inconsistently. |
| Automation command → amount limit/effect | Numeric tokens or old RPC signatures could bypass exact limits. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering / Repudiation | legacy backfill | mitigate | Validate-all first, safe row/path exceptions, transaction abort, no rounding/skip. |
| T-03-07 | Tampering | upgrade registry | mitigate | Ordered narrow categories, exact hashes/invariants, immutable prior artifacts. |
| T-03-08 | Tampering / Elevation | dual/RPC authority | mitigate | Exact-only write contract, revoke old numeric signature, derived projections, live tests. |
| T-03-05 | Elevation | replaced functions | mitigate | Preserve empty search path, qualification, exact grants, and catalog assertions. |
| T-03-SC | Tampering | migration/release | mitigate | Existing ordered local lanes and protected production promotion only. |
</threat_model>

<verification>
- `npm test -- --run tests/release/migration-upgrade.test.ts`
- `make test-financial-migration-upgrade`
- `make test-financial-schema-push`
- `make test-financial-database-sql`
</verification>

<success_criteria>
- Invoices, line items, and automation authority use exact minor units/rates.
- Any ambiguous historical row blocks the entire migration with safe evidence.
- Prior evidence stays immutable and compatibility fields cannot regain authority.
</success_criteria>

<output>Create `.planning/phases/03-exact-money-and-rounding-contract/03-03-SUMMARY.md` when done.</output>
