---
phase: 03-exact-money-and-rounding-contract
plan: "06"
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - src/components/atomic-crm/providers/fakerest/dataProvider.ts
  - src/components/atomic-crm/providers/fakerest/dataGenerator/billingAccounts.ts
  - src/components/atomic-crm/financial/exactProviderContract.test.ts
  - src/components/atomic-crm/invoices/invoiceCalculations.ts
  - src/components/atomic-crm/invoices/invoiceCalculations.test.ts
  - makefile
  - .github/release/financial-paths.json
  - tests/release/exact-money-release-static.test.ts
autonomous: true
requirements: [CALC-01, CALC-03]
must_haves:
  truths:
    - "D-22: FakeRest observes the same closed request, canonical response, stable error, and unchanged-effect behavior as the proven Supabase provider."
    - "D-20: Invoice preview delegates to the central BigInt exact module and exposes no authoritative `number`, fractional-cent, or independent rounding path."
    - "D-07: D-09: D-10: Canonical rates/policies remain embedded while submitted percentage is non-authoritative evidence and formatting-only history stays bounded to the existing audit contract."
    - "D-11: D-12: D-13: D-14: Exact invoice preview rounds once under named half-away-from-zero policy and rejects mismatch/overflow without coercion."
    - "FakeRest/provider parity and preview source/tests enter protected HTTP/fast/classifier contracts in this plan."
  artifacts:
    - path: src/components/atomic-crm/financial/exactProviderContract.test.ts
      provides: "One Supabase/FakeRest parity matrix for exact request/result/error behavior"
    - path: src/components/atomic-crm/invoices/invoiceCalculations.ts
      provides: "Exact-only invoice preview adapter over the central financial module"
    - path: src/components/atomic-crm/providers/fakerest/dataGenerator/billingAccounts.ts
      provides: "Deterministic synthetic exact fixtures without provider import cycles"
  key_links:
    - from: FakeRest invoice methods
      to: shared provider/financial codecs from Plans 01 and 05
      via: "identical closed request validation and canonical result construction"
    - from: invoiceCalculations.ts
      to: financial/exactMoney.ts
      via: "BigInt rational delegation; no independent numeric arithmetic"
    - from: Wave 6 tests
      to: FINANCIAL_DATABASE_HTTP_TESTS / FINANCIAL_FAST_TESTS
      via: "same-plan Make/classifier/static coupling"
---

<objective>
Complete provider parity and replace floating invoice preview.

Purpose: FakeRest or preview cannot remain a looser financial path after the live
Supabase boundary becomes exact.
Output: Deterministic exact FakeRest behavior, one cross-provider matrix, exact
invoice preview, and immediate protected-lane coupling.
</objective>

<decision_coverage>
- D-03 through D-10 and D-19: Exact canonical provider/line-item/evidence shapes.
- D-11 through D-14: Named one-boundary rounding and stable overflow/policy failures.
- D-20: BigInt-backed shared module replaces authoritative number helpers.
- D-22 and D-23: Real/fake parity, golden, malformed, full-range, and round-trip proof.
</decision_coverage>

<execution_context>
@.planning/phases/03-exact-money-and-rounding-contract/03-CONTEXT.md
@.planning/phases/03-exact-money-and-rounding-contract/03-RESEARCH.md
@.planning/phases/03-exact-money-and-rounding-contract/03-PATTERNS.md
@.planning/phases/03-exact-money-and-rounding-contract/03-VALIDATION.md
@.planning/phases/03-exact-money-and-rounding-contract/03-01-SUMMARY.md
@.planning/phases/03-exact-money-and-rounding-contract/03-05-SUMMARY.md
@src/components/atomic-crm/providers/fakerest/dataProvider.ts
@src/components/atomic-crm/providers/fakerest/dataGenerator/billingAccounts.ts
@src/components/atomic-crm/invoices/invoiceCalculations.ts
@src/components/atomic-crm/invoices/invoiceCalculations.test.ts
@makefile
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Match FakeRest to the proven exact Supabase contract</name>
  <files>src/components/atomic-crm/providers/fakerest/dataProvider.ts, src/components/atomic-crm/providers/fakerest/dataGenerator/billingAccounts.ts, src/components/atomic-crm/financial/exactProviderContract.test.ts</files>
  <read_first>
    - Plan 05 shared/provider types, exact Supabase list/get/save behavior, and stable errors
    - Existing FakeRest compound billing commands and generator/provider separation
    - Plan 04 canonical exact line-item schema, D-10 evidence fields, and caller-bound pagination semantics
  </read_first>
  <behavior>
    - One matrix observes identical list/get/save records, totals, pagination, canonicalization, error codes, and unchanged effects against Supabase and FakeRest.
    - FakeRest accepts only the closed list/get/save keys, enforces page/perPage and allowlisted filters/sorts, ignores no unknown key, and treats account filters only as narrowing input.
    - Exact line items round-trip with `quantity_ratio.{numerator,denominator}`, `unit_price.{amount_minor,currency}`, `extended_amount.{amount_minor,currency}`, `currency_policy_version`, and `rounding_policy_version`; `line_items_legacy_evidence` is never writable authority.
    - Full signed endpoints, canonical zero, 64/14 limits, and unsupported policies/currencies have provider-identical results; valid exact saves succeed, while unsafe numeric/formatted/invalid save input is rejected with invoice/audit effects unchanged.
  </behavior>
  <action>Write the shared parity matrix first, using the live Supabase methods from Plan 05 and a fresh deterministic FakeRest instance for each case. Implement FakeRest against the same exact codec and shared compound interface; keep synthetic exact records in `dataGenerator/billingAccounts.ts` and do not import the provider into the generator. Preserve the exact line-item names and map historical `quantity`, `rate`, and `amount` only through the migration—not as accepted FakeRest write aliases. Carry `submitted_percentage` as display evidence while financial equality uses the reduced ratio/policy. Exercise exact save success plus invalid-input rejection with unchanged invoice/audit effects; neither provider contract nor tests may add an invoice-save idempotency key, request fingerprint, or same-key save behavior, which remains deferred to Phase 5 `INV-01`. Reject browser tenant identity and unknown keys; do not simulate RLS by trusting organization/account input. The in-memory implementation may derive its fixture caller capability through its established provider context, but its externally observable contract must match Supabase exactly. Do not add agreement/formula/version/issuance/payment behavior.</action>
  <acceptance_criteria>
    - Identical success, list/get pagination, canonicalization, unsafe input, cross-scope, and unchanged-effect cases pass against both providers.
    - Provider save parity proves exact valid success and invalid-input zero effects without any invoice-save idempotency key/fingerprint or same-key save assertion.
    - FakeRest emits no JSON numeric financial component and accepts no legacy numeric alias.
    - The canonical line-item structure round-trips semantically while legacy evidence remains read-only.
  </acceptance_criteria>
  <verify><automated>node scripts/release/run-supabase-lane.mjs run --lane database-contracts -- npm test -- --run src/components/atomic-crm/financial/exactProviderContract.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <done>FakeRest and Supabase expose one exact, closed, and testably identical financial provider contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace floating invoice preview with named exact arithmetic</name>
  <files>src/components/atomic-crm/invoices/invoiceCalculations.ts, src/components/atomic-crm/invoices/invoiceCalculations.test.ts</files>
  <read_first>
    - Existing number-based invoice helper signatures and fractional expectations
    - Plan 01 exact money/rate/rounding API and deterministic fixtures
    - Plan 04 canonical line-item fields and embedded policy identities
  </read_first>
  <behavior>
    - Preview accepts only exact quantity ratios, typed USD money, canonical rates, and explicit `usd-v1` / `half-away-from-zero-v1` identities.
    - `8.875%` on `10000` minor units yields tax `888` and total `10888`; positive/negative half ties mirror exactly and zero has no negative form.
    - Intermediate ratios remain exact until one declared minor-unit conversion; policy/currency mismatch, denominator zero, and final range overflow fail with stable safe codes.
    - Old fractional `887.5`, `Math.round`, `toFixed`, `parseFloat`, `Number(amount_minor)`, and authoritative `number` signatures are absent.
  </behavior>
  <action>Write replacement golden/signed/policy/overflow tests first, then reduce `invoiceCalculations.ts` to a narrow adapter over `financial/exactMoney.ts`. Accept the canonical line-item/rate objects, use BigInt rational intermediates, call the named rounding boundary once, and return typed money objects plus non-authoritative human descriptions. Do not duplicate parsing, GCD, range, or rounding logic. Keep D-10 submitted text as display evidence only and do not create a formula/version snapshot. If no rendered screen consumes this helper, do not add UI or surface receipts in Phase 3.</action>
  <acceptance_criteria>
    - Exact golden, signed tie, mismatch, denominator, and endpoint/overflow tests pass without number arithmetic.
    - Static search of exact/preview/provider authority finds none of the prohibited float helpers or authoritative number signatures.
    - Human-facing descriptions cannot be parsed back into authority.
  </acceptance_criteria>
  <verify><automated>npm test -- --run src/components/atomic-crm/invoices/invoiceCalculations.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <done>Invoice preview is a faithful view of the shared named exact contract rather than a second calculator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Protect provider parity and preview before final audit</name>
  <files>makefile, .github/release/financial-paths.json, tests/release/exact-money-release-static.test.ts</files>
  <read_first>
    - Completed provider parity and invoice preview source/test paths
    - Current `FINANCIAL_DATABASE_HTTP_TESTS`, `FINANCIAL_FAST_TESTS`, and path classifier
    - Rolling Plan 01–05 assertions and inherited six financial workflow identities
  </read_first>
  <behavior>
    - `exactProviderContract.test.ts` runs in the protected isolated database HTTP target; `invoiceCalculations.test.ts` runs in the protected fast target.
    - FakeRest/generator, provider parity, and invoice preview paths classify as financial in the plan that makes them exact-money bearing.
    - Static proof fails on removed membership, demo-only numeric behavior, preview float helpers, or optional/replacement lanes.
  </behavior>
  <action>Add failing rolling assertions for every Plan 06 path and target before editing Make/classifier configuration. Extend the existing HTTP/fast lists and narrow classifier without renaming targets, duplicating tests, weakening isolation, changing the six required identities, or adding assertion retry. Run both permanent targets. Plan 07 remains a final audit, not the first coupling point. This closes C2-W2's FakeRest/preview half and preserves H3/M1.</action>
  <acceptance_criteria>
    - Every Plan 06 money-bearing source/test is classified and its focused test is an explicit applicable protected-target member.
    - Removing either test or reintroducing float authority fails the static contract.
    - Permanent HTTP and fast targets execute the new tests successfully.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/exact-money-release-static.test.ts &amp;&amp; make test-financial-database-http &amp;&amp; make test-financial-fast</automated></verify>
  <done>FakeRest/provider/preview exactness cannot land or remain without its protected behavior tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Shared provider request → FakeRest | Demo code could trust tenant input or accept looser financial tokens. |
| Exact line items → provider round trip | Field drift could swap quantity, unit price, or extended authority. |
| Exact values → invoice preview | Independent number arithmetic could disagree with database policy. |
| New provider/preview paths → CI | Explicit target lists could omit the new proof. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | FakeRest tokens | mitigate | Shared codec/matrix, exact strings, full-range/malformed tests. |
| T-03-02 | Tampering | invoice preview | mitigate | Central BigInt helper delegation and float anti-pattern checks. |
| T-03-03 | Repudiation | named preview policy | mitigate | Explicit policy/currency inputs and signed golden tests. |
| T-03-15 | Elevation | FakeRest caller scope | mitigate | Provider-context capability and rejection of client tenant identity. |
| T-03-09 | Tampering / Repudiation | Wave 6 coupling | mitigate | Same-plan Make/classifier/static updates and permanent targets. |
| T-03-SC | Tampering | provider dependencies | mitigate | No package changes; existing locked clients/toolchain only. |
</threat_model>

<verification>
- `node scripts/release/run-supabase-lane.mjs run --lane database-contracts -- npm test -- --run src/components/atomic-crm/financial/exactProviderContract.test.ts`
- `npm test -- --run src/components/atomic-crm/invoices/invoiceCalculations.test.ts`
- `npm run typecheck`
- `make test-financial-database-http`
- `make test-financial-fast`
- `npm test -- --run tests/release/exact-money-release-static.test.ts`
- `git diff --check`
</verification>

<success_criteria>
- Supabase and FakeRest accept, reject, paginate, and serialize exact financial values identically.
- Invoice preview contains no floating-point financial authority.
- Exact line-item and D-10 evidence semantics remain unchanged.
- Every Wave 6 path/test is protected in Wave 6.
</success_criteria>

<output>Create `.planning/phases/03-exact-money-and-rounding-contract/03-06-SUMMARY.md` when done.</output>
