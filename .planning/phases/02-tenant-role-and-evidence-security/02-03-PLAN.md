---
phase: 02-tenant-role-and-evidence-security
plan: "03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - tests/release/billing-tenancy.test.ts
autonomous: true
requirements: [WORK-01, SEC-02, SEC-03, SEC-06]
must_haves:
  truths:
    - "Real users in two organizations cannot read or mutate the other organization's account, contacts, assignments, audit, or invoices."
    - "Admin, operator, reviewer, auditor, and customer capabilities differ exactly as assigned."
    - "Denied HTTP operations have zero protected effects and safe responses."
  artifacts:
    - path: tests/release/billing-tenancy.test.ts
      provides: "Real local Auth JWT/PostgREST/RPC two-tenant acceptance matrix"
  key_links:
    - from: "local Auth sessions"
      to: "PostgREST billing resources"
      via: "Bearer JWTs created through signup/login"
    - from: "denied HTTP requests"
      to: "database effect counts"
      via: "service-only postcondition queries"
---

<objective>
Prove the human/account boundary externally with real Auth sessions and exact effects.

Purpose: Internal claims tests alone cannot prove PostgREST and JWT integration.
Output: A reusable two-tenant HTTP registry for all later billing resources.
</objective>

<execution_context>
@.planning/phases/02-tenant-role-and-evidence-security/02-CONTEXT.md
@.planning/phases/02-tenant-role-and-evidence-security/02-PATTERNS.md
@tests/release/auth-rls-rpc-trigger.test.ts
@supabase/tests/support/billing-security-fixtures.sql
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exercise the real human role and two-tenant read matrix</name>
  <files>tests/release/billing-tenancy.test.ts</files>
  <read_first>
    - `tests/release/auth-rls-rpc-trigger.test.ts` for local guard, real JWT, and cleanup patterns
    - `supabase/tests/support/billing-security-fixtures.sql` and Plans 01/02 summaries
  </read_first>
  <behavior>
    - Local Auth signs up/logs in distinct admin, operator, reviewer, auditor, and customer users across two organizations.
    - Same-account reads match capabilities; cross-account and unauthenticated reads return no protected rows.
    - Tokens stay in memory and are invalidated during cleanup.
  </behavior>
  <action>Follow the exact local-URL guard and real-principal pattern from `auth-rls-rpc-trigger.test.ts`. Build a data-driven D-16 registry whose cases name principal, organization, account, resource, operation, expected visibility, and expected result. Resolve server-created sales identities without printing JWTs or contact details. Cover accounts, owners, contacts, assignments, support-safe audit view, and invoices. No mocked JWT or source-string assertion may satisfy a case.</action>
  <acceptance_criteria>
    - Test refuses non-loopback API URLs and missing anon key before creating a user.
    - Registry covers all five human roles in each organization across every named resource.
    - Cross-tenant reads expose zero protected rows and every created Auth user/session is cleaned up.
  </acceptance_criteria>
  <verify><automated>node scripts/release/run-supabase-lane.mjs run --lane database-contracts -- npm test -- --run tests/release/billing-tenancy.test.ts -t human</automated></verify>
  <done>Real local sessions prove same-scope capability reads and complete cross-tenant invisibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove mutation denials and exact audit/effect postconditions</name>
  <files>tests/release/billing-tenancy.test.ts</files>
  <read_first>
    - `tests/release/billing-tenancy.test.ts` Task 1 registry and helpers
    - Plan 01 audit schema and Plan 02 invoice RLS
  </read_first>
  <behavior>
    - Authorized account/contact/status/assignment changes succeed and append one expected audit event.
    - Cross-tenant, wrong-role, browser-authored tenant, and hard-delete attempts have zero target/audit effects.
    - Public responses never disclose existence, raw policy details, or sensitive fields.
  </behavior>
  <action>Add effect-focused cases for D-02, D-06, D-09, D-13, and D-15. Snapshot counts/IDs through the service-only test boundary, perform real REST/RPC mutations, then assert exact final rows and audit events. Include account/contact soft-ending and reject hard delete. Assert safe stable error shapes but make unchanged state the denial authority.</action>
  <acceptance_criteria>
    - Each authorized mutation has the expected row change and exactly one allowlisted audit event.
    - Each forbidden/cross-tenant/browser-authored-scope/delete case leaves target and audit counts exact.
    - Public responses and test output contain no JWT, contact details, policy internals, or evidence paths.
  </acceptance_criteria>
  <verify><automated>node scripts/release/run-supabase-lane.mjs run --lane database-contracts -- npm test -- --run tests/release/billing-tenancy.test.ts -t effects</automated></verify>
  <done>Every authorized and denied human operation has an exact externally proven database outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Auth client → PostgREST | Real JWTs exercise the public resource boundary. |
| HTTP response → test logs | Tokens, contacts, and policy details could leak during failure. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Spoofing / Elevation | HTTP tenant matrix | mitigate | Real distinct users, server-derived scope, exact cross-tenant effects. |
| T-02-07 | Information Disclosure | assertion output | mitigate | In-memory tokens, stable messages, redacted diagnostics, cleanup. |
| T-02-SC | Tampering | dependencies | mitigate | No install; existing pinned test stack. |
</threat_model>

<verification>
- Run both targeted test names through the isolated database-contracts lane.
</verification>

<success_criteria>
- Two real tenants and all five human roles are exercised externally.
- Cross-tenant and forbidden-role mutations have zero effects.
- Test output contains no tokens, credentials, or sensitive evidence paths.
</success_criteria>

<output>Create `.planning/phases/02-tenant-role-and-evidence-security/02-03-SUMMARY.md` when done.</output>
