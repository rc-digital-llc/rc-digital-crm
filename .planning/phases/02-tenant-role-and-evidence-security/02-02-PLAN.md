---
phase: 02-tenant-role-and-evidence-security
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - supabase/migrations/20260901000002_billing_invoice_boundary.sql
  - supabase/tests/upgrades/002-billing-tenancy/expected-transformations.json
  - scripts/release/fingerprint-upgrade.mjs
  - tests/release/migration-upgrade.test.ts
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-06]
must_haves:
  truths:
    - "Every existing invoice belongs to one explicit organization and billing account after upgrade."
    - "An ambiguous or unmapped hosted invoice blocks promotion instead of receiving guessed ownership."
    - "The accepted pre-financial baseline stays immutable while the intentional transform is independently recorded."
  artifacts:
    - path: supabase/migrations/20260901000002_billing_invoice_boundary.sql
      provides: "Validated invoice organization/account backfill and tenant-safe policy replacement"
    - path: supabase/tests/upgrades/002-billing-tenancy/expected-transformations.json
      provides: "Append-only accepted upgrade transformation contract"
  key_links:
    - from: public.invoices
      to: public.billing_accounts
      via: "validated organization_id and billing_account_id foreign keys"
    - from: scripts/release/fingerprint-upgrade.mjs
      to: supabase/tests/upgrades/002-billing-tenancy/expected-transformations.json
      via: "exact before/after transformation verification"
---

<objective>
Bring inherited invoices inside the new boundary without mutating accepted baseline evidence.

Purpose: Existing billing facts cannot remain protected only by salesperson ownership.
Output: A deterministic expand/validate/contract migration and append-only upgrade proof.
</objective>

<execution_context>
@.planning/phases/02-tenant-role-and-evidence-security/02-CONTEXT.md
@.planning/phases/02-tenant-role-and-evidence-security/02-RESEARCH.md
@.planning/phases/02-tenant-role-and-evidence-security/02-PATTERNS.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-03-SUMMARY.md
@scripts/release/fingerprint-upgrade.mjs
@tests/release/migration-upgrade.test.ts
@supabase/migrations/20260305000004_add_invoices_table.sql
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend upgrade proof with an append-only transformation registry</name>
  <files>supabase/tests/upgrades/002-billing-tenancy/expected-transformations.json, scripts/release/fingerprint-upgrade.mjs, tests/release/migration-upgrade.test.ts</files>
  <read_first>
    - `scripts/release/fingerprint-upgrade.mjs` and `tests/release/migration-upgrade.test.ts`
    - `supabase/tests/baselines/001-pre-financial/manifest.json` and `expected-fingerprints.json` as immutable inputs
    - `01-03-SUMMARY.md` for append-only baseline authority
  </read_first>
  <behavior>
    - Baseline 001 hashes and files remain byte-identical.
    - A numbered registry entry binds each allowed Phase 2 before/after category hash and semantic invariant.
    - Unknown, edited, stale, or overbroad transformation entries fail closed.
  </behavior>
  <action>Preserve Phase 1's immutable baseline rule while teaching the upgrade runner to load ordered, schema-validated transformation registries outside accepted baseline directories. Record only intentional additions/backfills from D-01 and D-03; continue exact text handling for numeric invoice values and reject transformations that omit before hashes, name unknown categories, overlap, or fail semantic row/count/foreign-key checks. Add focused Vitest cases before implementing the registry loader.</action>
  <acceptance_criteria>
    - No file under `supabase/tests/baselines/001-pre-financial` changes.
    - Registry validation rejects missing/unknown/overlapping or stale hashes and accepts the ordered 002 contract.
    - `npm test -- --run tests/release/migration-upgrade.test.ts` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/migration-upgrade.test.ts</automated></verify>
  <done>The upgrade lane can authorize this explicit schema/backfill transform without weakening or editing baseline 001.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backfill and constrain invoice tenant ownership</name>
  <files>supabase/migrations/20260901000002_billing_invoice_boundary.sql, supabase/tests/upgrades/002-billing-tenancy/expected-transformations.json</files>
  <read_first>
    - `supabase/migrations/20260305000004_add_invoices_table.sql` and the Plan 01 migration
    - `supabase/tests/baselines/001-pre-financial/fixtures.sql` for representative inherited invoice relationships
    - Task 1 transformation registry/loader contract
  </read_first>
  <behavior>
    - Each historical invoice maps through its company to exactly one account in the default RC Digital organization.
    - Non-administrator invoice owners receive only the account-scoped operator assignment needed for compatibility.
    - Zero/duplicate mappings abort before NOT NULL constraints or new policies are committed.
  </behavior>
  <action>Implement D-01 and D-03 as an expand/backfill/validate/contract migration: add nullable organization/account keys, create only the accounts referenced by invoices, preserve responsible owner facts, add narrowly scoped compatibility assignments, assert exact counts and uniqueness, set keys NOT NULL, add indexes/FKs, and replace legacy invoice policies with capability-bound force RLS. Do not change amounts, currency, provider facts, or implement Phase 3/5 behavior. Compute and commit the exact numbered upgrade registry result. [BLOCKING] Run both the full isolated upgrade proof and the isolated schema-push proof after the migration; never target the dashboard project directly.</action>
  <acceptance_criteria>
    - Migration aborts when an invoice maps to zero or multiple accounts before constraints/policies commit.
    - Every representative invoice gains non-null valid organization/account keys while amount/currency/provider text fingerprints stay exact.
    - Both isolated upgrade and schema-push commands exit 0.
  </acceptance_criteria>
  <verify><automated>make test-financial-migration-upgrade &amp;&amp; make test-financial-schema-push</automated></verify>
  <done>Existing invoices have exact tenant/account ownership, unmapped data fails closed, numeric facts are preserved, and the transform is receipt-ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Legacy owner/company → billing account | Ambiguous historical relationships could be guessed during backfill. |
| Transformation registry → upgrade authority | An overbroad exception could legitimize unrelated data changes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering / Disclosure | invoice backfill | mitigate | Exact mapping assertions, NOT NULL/FKs only after validation, no fallback guess. |
| T-02-05 | Tampering | upgrade registry | mitigate | Ordered schema, exact before/after hashes, semantic invariants, immutable baseline. |
| T-02-SC | Tampering | dependencies | mitigate | No package install; Phase 1 supply-chain checks continue. |
</threat_model>

<verification>
- `npm test -- --run tests/release/migration-upgrade.test.ts`
- `make test-financial-migration-upgrade`
- `make test-financial-schema-push`
</verification>

<success_criteria>
- Every inherited invoice maps once and only once.
- Upgrade proof distinguishes approved tenant-column changes from forbidden fact drift.
- Existing invoice numerics and provider facts remain exact.
</success_criteria>

<output>Create `.planning/phases/02-tenant-role-and-evidence-security/02-02-SUMMARY.md` when done.</output>
