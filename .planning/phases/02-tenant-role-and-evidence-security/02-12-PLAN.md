---
phase: 02-tenant-role-and-evidence-security
plan: "12"
type: execute
wave: 6
depends_on: ["02-03", "02-04", "02-07", "02-10", "02-11"]
files_modified:
  - makefile
  - .github/workflows/financial-release-gate.yml
  - .github/release/financial-paths.json
  - tests/release/billing-security-static.test.ts
  - .planning/phases/02-tenant-role-and-evidence-security/02-VALIDATION.md
autonomous: true
requirements: [WORK-01, SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07]
must_haves:
  truths:
    - "Every Phase 2 database, Auth, automation, Storage, Edge, redaction, provider, and UI contract runs through blocking existing check identities."
    - "Financial path classification cannot let billing security or UI changes skip protected gates."
    - "The exact source build passes full financial/type/lint/build validation and retains a rendered source receipt before PR readiness."
  artifacts:
    - path: makefile
      provides: "Phase 2 tests included in stable local financial targets"
    - path: .github/workflows/financial-release-gate.yml
      provides: "Existing immutable protected check identities execute expanded contracts"
    - path: .planning/phases/02-tenant-role-and-evidence-security/02-VALIDATION.md
      provides: "Final task-to-proof results and residual manual coverage record"
  key_links:
    - from: .github/release/financial-paths.json
      to: .github/workflows/financial-release-gate.yml
      via: "billing schema/functions/UI paths classify as financial"
    - from: makefile financial lanes
      to: Phase 2 test files
      via: "explicit non-optional Vitest/pgTAP file lists"
---

<objective>
Close Phase 2 through the inherited protected gate and rendered source proof.

Purpose: New security behavior is incomplete if it is optional, unclassified, or absent from exact-commit release evidence.
Output: Expanded stable lanes, path classification, final static coupling tests, source receipt, and validation results.
</objective>

<execution_context>
@.planning/phases/02-tenant-role-and-evidence-security/02-VALIDATION.md
@.planning/phases/02-tenant-role-and-evidence-security/02-PATTERNS.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-10-SUMMARY.md
@makefile
@.github/workflows/financial-release-gate.yml
@.github/release/financial-paths.json
@qa/billing-accounts.surface.source.json
@scripts/release/run-billing-source-surface.mjs
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Couple every Phase 2 contract into existing blocking lane identities</name>
  <files>makefile, .github/workflows/financial-release-gate.yml, .github/release/financial-paths.json, tests/release/billing-security-static.test.ts</files>
  <read_first>
    - Existing `makefile` financial targets and `.github/workflows/financial-release-gate.yml`
    - `.github/release/financial-paths.json` and Phase 1 release-policy tests
    - Every Phase 2 test filename from `02-VALIDATION.md`
  </read_first>
  <behavior>
    - Database HTTP runs both inherited and billing tenancy tests; Edge runs inherited and billing evidence tests; fast tests cover redaction/provider/UI/static contracts.
    - Billing migrations/functions/shared authorization/UI/QA paths classify as financial and cannot skip merge-group lanes.
    - No optional job, continue-on-error, alternate check name, assertion retry, or direct remote mutation is introduced.
  </behavior>
  <action>Add source-coupling tests first, then extend existing Make targets and the exact financial workflow jobs rather than introducing new bypassable contexts. Preserve the six required financial identities, unconditional merge_group behavior, CLI pin, timeouts, Phase 1 isolated runner, cleanup, zero assertion retry, and protected release policy. Expand financial path globs narrowly enough to cover every Phase 2 authority/UI/QA file. Run billing redaction/provider/UI static tests in an existing fast required check if that check is the current authority; keep live database/Edge tests inside their correct isolated lanes.</action>
  <acceptance_criteria>
    - Existing required financial check names/count and merge_group triggers remain exact.
    - Make targets explicitly execute all Phase 2 SQL/Auth/Edge/redaction/provider/UI/static files in their proper lanes.
    - Classification tests prove each Phase 2 schema/function/shared-auth/UI/QA path is financial and reject skip/optional/retry/direct-remote patterns.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/billing-security-static.test.ts -t lanes</automated></verify>
  <done>No Phase 2 protected behavior or rendered contract can merge through an unclassified or optional path.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete phase gate and retain the source surface receipt</name>
  <files>.planning/phases/02-tenant-role-and-evidence-security/02-VALIDATION.md</files>
  <read_first>
    - `02-VALIDATION.md` complete per-task map and residual verification table
    - `qa/billing-accounts.surface.source.json`
    - Build-gate `surface-loop.md` exact source command/receipt rules
  </read_first>
  <action>Run the full validation command without assertion retries. Start the exact source build with a disposable local Supabase/FakeRest configuration suitable for deterministic account fixtures, then run `surface_gate.py` using the source contract, local base URL, `--stage source`, a Phase 2 receipt path, screenshots directory, and `--json`. Require both source viewports, the intended freshness marker, route/canonical identity, reflow, touch/fixed/overlay/focus/browser-error checks. Record each task result and receipt/hash location in VALIDATION.md. If Playwright/browser is unavailable or any receipt check fails, stop as blocked; do not convert it to manual approval.</action>
  <acceptance_criteria>
    - Full financial/type/lint/build command exits 0 at the exact task commit without assertion retry.
    - Source receipt status is passed for both contract viewports and contains screenshot hashes plus `billing-security-phase2` freshness proof.
    - VALIDATION.md maps every task to green evidence or a named blocker and does not claim preview/production/manual coverage prematurely.
  </acceptance_criteria>
  <verify><automated>make financial-gate &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; node scripts/release/run-billing-source-surface.mjs run --stage source --contract qa/billing-accounts.surface.source.json --receipt .planning/evidence/02/source/receipt.json --screenshots .planning/evidence/02/source/screenshots</automated></verify>
  <done>All automated phase checks are green and a machine-readable rendered source receipt exists before the PR is marked ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Changed path → CI classification | Misclassification could skip live security checks. |
| Local/source receipt → PR readiness | A source diff/build could be mistaken for rendered or deployed proof. |
| Protected merge → release | Merge success alone does not prove preview or production surfaces. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-28 | Tampering / Repudiation | path/check coupling | mitigate | Closed static contract plus existing exact required identities. |
| T-02-29 | Spoofing | source receipt | mitigate | Exact local base/route/marker and hashed screenshots; preview/production remain separately required. |
| T-02-30 | Elevation | release path | mitigate | Phase 1 protected staged promotion and owner approvals unchanged. |
| T-02-SC | Tampering | workflow supply chain | mitigate | Existing full-SHA actions and pinned Supabase CLI; no install changes. |
</threat_model>

<verification>
- `npm test -- --run tests/release/billing-security-static.test.ts`
- `make financial-gate && npm run typecheck && npm run lint && npm run build`
- Rendered `source` receipt passes before PR readiness.
- Immutable `preview` receipt passes before merge and independent `production` receipt passes after authorized promotion; these release-stage receipts cannot be pre-satisfied locally.
</verification>

<success_criteria>
- Phase 2 is enforced by inherited non-bypassable financial checks.
- All implementation tasks have executable green evidence.
- Source receipt exists now; preview/production remain explicit release gates before ready/live claims.
</success_criteria>

<output>Create `.planning/phases/02-tenant-role-and-evidence-security/02-12-SUMMARY.md` when done.</output>
