---
phase: 02-tenant-role-and-evidence-security
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/redaction.ts
  - supabase/functions/_shared/utils.ts
  - tests/release/billing-redaction.test.ts
autonomous: true
requirements: [SEC-07]
must_haves:
  truths:
    - "D-13: Logs and public errors cannot emit secrets, credentials, signed URLs/tokens, sensitive provider payloads, contact details, or evidence paths."
    - "Nested arrays/objects, unusual casing, URL query strings, and Error objects are redacted recursively."
    - "Callers receive stable operation messages while internal logs contain only allowlisted context."
  artifacts:
    - path: supabase/functions/_shared/redaction.ts
      provides: "Recursive allowlist/denylist redaction and safe operation context"
      exports: [redactSensitiveValue, createSafeLogContext]
    - path: tests/release/billing-redaction.test.ts
      provides: "Negative fixtures for every prohibited D-13 field family"
  key_links:
    - from: supabase/functions/_shared/utils.ts
      to: supabase/functions/_shared/redaction.ts
      via: "safe error/log boundary helpers"
---

<objective>
Create a reusable redaction boundary before new privileged Edge commands can log or respond.

Purpose: Sensitive data must be excluded at emission, not scrubbed after broad logging.
Output: Dependency-free shared redaction utilities and focused Vitest contracts.
</objective>

<decision_coverage>
- D-13: Logs, errors, support context, and later exports use explicit safe allowlists.
</decision_coverage>

<execution_context>
@.planning/phases/02-tenant-role-and-evidence-security/02-CONTEXT.md
@.planning/phases/02-tenant-role-and-evidence-security/02-PATTERNS.md
@scripts/release/run-supabase-lane.mjs
@supabase/functions/_shared/utils.ts
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build recursive value and URL redaction from negative fixtures</name>
  <files>supabase/functions/_shared/redaction.ts, tests/release/billing-redaction.test.ts</files>
  <read_first>
    - `scripts/release/run-supabase-lane.mjs` redaction helper behavior
    - `02-CONTEXT.md` D-13 and `02-RESEARCH.md` sensitive logging threat pattern
    - Existing `supabase/functions/_shared/utils.ts` public error shape
  </read_first>
  <behavior>
    - Primitive/nested arrays and objects remove or replace prohibited keys case-insensitively.
    - Authorization/JWT/API keys, passwords, credentials, signed URL query data, provider payloads, email/phone, object path, and evidence contents never survive.
    - Cycles, Error objects, unknown values, and excessive depth produce stable safe markers without throwing.
  </behavior>
  <action>Write D-13 negative tests first, then implement a dependency-free recursive helper. Use an explicit allowlist for operation context and a conservative denylist for nested values; strip URL query/fragment data and never serialize arbitrary Error/request/Response objects. Preserve only safe scalar diagnostics such as operation code, result, reason code, and opaque request correlation ID. Reuse the Phase 1 redaction intent but provide a Deno/browser-testable TypeScript module.</action>
  <acceptance_criteria>
    - Test fixtures cover every prohibited field family plus mixed case, nested arrays, cycles, Error objects, URLs, and depth limits.
    - Redacted results retain only documented safe scalars and never throw on unknown input.
    - Focused recursive tests exit 0 and contain no realistic secret/customer value.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/billing-redaction.test.ts -t recursive</automated></verify>
  <done>Every prohibited sensitive field family is covered by a failing-then-passing recursive fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route shared error and logging helpers through safe context</name>
  <files>supabase/functions/_shared/utils.ts, tests/release/billing-redaction.test.ts</files>
  <read_first>
    - `supabase/functions/_shared/utils.ts` and Task 1 `redaction.ts`
    - Existing function callers found by importing `createErrorResponse`
  </read_first>
  <behavior>
    - Public error responses use stable status/code/message values without internal exception text.
    - Structured log context accepts only named safe fields and is deterministically redacted.
    - Existing callers remain source-compatible unless they attempt unsafe broad context.
  </behavior>
  <action>Add boundary tests, then extend shared utilities with a stable safe error constructor/log-context helper backed by `redaction.ts`. Do not log inside the helper, accept raw bodies, interpolate target IDs into error messages, or expose stack/database/provider messages. Keep existing `createErrorResponse` behavior compatible for unrelated functions while new billing commands use the hardened path.</action>
  <acceptance_criteria>
    - Public billing error helper accepts only status/code/stable message and emits JSON without internal details.
    - Safe context helper rejects/strips raw bodies, errors, tokens, URLs, identifiers, contact and provider fields.
    - Existing unrelated `createErrorResponse` imports typecheck and boundary tests exit 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/billing-redaction.test.ts -t boundary</automated></verify>
  <done>New Edge code has one tested safe emission path and cannot accidentally serialize broad inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Request/error object → logs | Arbitrary nested secrets can cross into operational output. |
| Internal failure → HTTP response | Database/provider details could disclose identities or controls. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Information Disclosure | recursive logger | mitigate | Negative fixture families, depth/cycle handling, allowlisted context. |
| T-02-14 | Information Disclosure | public errors | mitigate | Stable codes/messages, no raw exceptions or identifiers. |
| T-02-SC | Tampering | dependencies | mitigate | Dependency-free implementation; no install. |
</threat_model>

<verification>
- `npm test -- --run tests/release/billing-redaction.test.ts`
</verification>

<success_criteria>
- Recursive redaction is deterministic and fail-safe.
- New public errors/logs expose only allowlisted context.
- No new dependency or secret fixture is introduced.
</success_criteria>

<output>Create `.planning/phases/02-tenant-role-and-evidence-security/02-06-SUMMARY.md` when done.</output>
