---
phase: 02-tenant-role-and-evidence-security
plan: "08"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/atomic-crm/types.ts
  - src/components/atomic-crm/providers/types.ts
  - src/components/atomic-crm/providers/supabase/dataProvider.ts
  - src/components/atomic-crm/providers/fakerest/dataProvider.ts
  - src/components/atomic-crm/providers/fakerest/dataGenerator/types.ts
  - src/components/atomic-crm/providers/fakerest/dataGenerator/billingAccounts.ts
  - src/components/atomic-crm/providers/fakerest/dataGenerator/index.ts
  - src/components/atomic-crm/billing-accounts/billingDataProvider.test.ts
autonomous: true
requirements: [WORK-01, SEC-03, SEC-07]
must_haves:
  truths:
    - "The UI can load and maintain billing accounts, responsible owners, contacts, and scoped access through typed provider contracts."
    - "Supabase and FakeRest expose equivalent safe behavior, while tenant identity remains server-derived in production."
    - "Compound evidence/access commands are explicit methods rather than hidden generic mutations."
  artifacts:
    - path: src/components/atomic-crm/types.ts
      provides: "Billing account/contact/owner/role/automation/evidence record types"
    - path: src/components/atomic-crm/providers/supabase/dataProvider.ts
      provides: "Live CRUD and explicit billing command adapter"
    - path: src/components/atomic-crm/providers/fakerest/dataGenerator/billingAccounts.ts
      provides: "Deterministic no-secret demo fixtures"
  key_links:
    - from: CrmDataProvider
      to: billing_evidence Edge function
      via: "typed explicit evidence capability methods"
    - from: fakerest dataProvider
      to: billingDataProvider.test.ts
      via: "behavioral parity contract"
---

<objective>
Define and implement the dual-provider billing data contract consumed by the account UI.

Purpose: Both live Supabase and demo FakeRest modes must remain usable without leaking server authority into browser fields.
Output: Types, provider methods, deterministic fixtures, and parity tests.
</objective>

<execution_context>
@.planning/phases/02-tenant-role-and-evidence-security/02-CONTEXT.md
@.planning/phases/02-tenant-role-and-evidence-security/02-PATTERNS.md
@src/components/atomic-crm/providers/supabase/dataProvider.ts
@src/components/atomic-crm/providers/fakerest/dataProvider.ts
@src/components/atomic-crm/providers/fakerest/dataGenerator/index.ts
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define billing records and live provider commands</name>
  <files>src/components/atomic-crm/types.ts, src/components/atomic-crm/providers/types.ts, src/components/atomic-crm/providers/supabase/dataProvider.ts, src/components/atomic-crm/billing-accounts/billingDataProvider.test.ts</files>
  <read_first>
    - Existing domain types and `providers/types.ts`
    - `providers/supabase/dataProvider.ts` custom attachment/merge patterns
    - Plans 01/05/07 summaries for exact record and command contracts
  </read_first>
  <behavior>
    - Standard account/owner/contact/assignment/principal/grant/evidence metadata records are typed without `any`.
    - Standard CRUD delegates through existing Supabase provider semantics and never injects organization ID from browser configuration.
    - Evidence upload/download/inspection and lifecycle commands have explicit typed request/response shapes with allowlisted fields.
  </behavior>
  <action>Write provider contract tests, then extend domain/provider types and the Supabase-derived `CrmDataProvider`. Use standard inherited CRUD for tables and explicit custom methods only for compound server commands. Request shapes may identify account/evidence/purpose but must not accept organization, object path, provider secret, signed token, or arbitrary details. Do not change legacy CRM ownership behavior per D-03. Preserve existing attachment behavior for nonfinancial CRM records; financial evidence uses only the new function.</action>
  <acceptance_criteria>
    - Billing record and command types contain no `any` and match server fields/discriminants exactly.
    - Provider request types have no organization/path/secret/token/arbitrary-details authority fields.
    - Contract tests and `npm run typecheck` resolve all live adapter methods without changing nonfinancial attachments.
  </acceptance_criteria>
  <verify><automated>npm test -- --run src/components/atomic-crm/billing-accounts/billingDataProvider.test.ts -t contracts</automated></verify>
  <done>Live provider types are explicit, safe, and sufficient for all Phase 2 account/evidence UI flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic FakeRest parity without secrets or cycles</name>
  <files>src/components/atomic-crm/providers/fakerest/dataProvider.ts, src/components/atomic-crm/providers/fakerest/dataGenerator/types.ts, src/components/atomic-crm/providers/fakerest/dataGenerator/billingAccounts.ts, src/components/atomic-crm/providers/fakerest/dataGenerator/index.ts, src/components/atomic-crm/billing-accounts/billingDataProvider.test.ts</files>
  <read_first>
    - Existing FakeRest generator `types.ts`, `index.ts`, and `finalize.ts`
    - Existing FakeRest dataProvider custom methods and the Task 1 provider interface
    - `AGENTS.md` FakeRest circular-import prohibition
  </read_first>
  <behavior>
    - Demo mode returns deterministic account/contact/access/evidence metadata and mirrors lifecycle/status outcomes.
    - Demo signed-capability methods return visibly synthetic non-secret responses and cannot expose permanent/public URLs.
    - Generator/provider dependencies do not extend the existing circular import.
  </behavior>
  <action>Follow the mapped FakeRest generator/finalize pattern. Generate reserved-example names/emails and opaque IDs; no customer data, credentials, provider references, raw paths, or realistic signed secrets. Implement the same `CrmDataProvider` methods with deterministic authorization/status facsimiles adequate for UI behavior, and assert method/resource parity against the Supabase adapter. Keep generator types independent from the dataProvider module to avoid the documented cycle.</action>
  <acceptance_criteria>
    - FakeRest resource/method keys equal the Supabase contract and every fixture is deterministic reserved-example data.
    - Synthetic evidence capability is visibly non-secret, short-lived in shape, and never a public/permanent URL.
    - Dependency scan shows no generator import from dataProvider and parity tests/typecheck exit 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --run src/components/atomic-crm/billing-accounts/billingDataProvider.test.ts -t parity</automated></verify>
  <done>Demo and live providers satisfy one typed contract without browser tenant authority or sensitive fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI values → provider request | Browser-controlled tenant/path/provider fields could be forwarded as authority. |
| Fake data → repository/build | Demo fixtures could accidentally resemble secrets or customer data. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Spoofing / Elevation | provider contracts | mitigate | Explicit allowlisted request types; server derives organization/path/authority. |
| T-02-19 | Information Disclosure | FakeRest fixtures | mitigate | Reserved-example values, synthetic capabilities, parity/redaction tests. |
| T-02-SC | Tampering | dependencies | mitigate | Existing provider stack only; no registry install. |
</threat_model>

<verification>
- `npm test -- --run src/components/atomic-crm/billing-accounts/billingDataProvider.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- UI contracts are typed and provider-neutral.
- Browser requests cannot author tenant, path, or provider authority.
- FakeRest parity is deterministic and safe.
</success_criteria>

<output>Create `.planning/phases/02-tenant-role-and-evidence-security/02-08-SUMMARY.md` when done.</output>
