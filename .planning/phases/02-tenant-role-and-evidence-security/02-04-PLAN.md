---
phase: 02-tenant-role-and-evidence-security
plan: "04"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - supabase/migrations/20260901000003_billing_automation_grants.sql
  - supabase/tests/database/35_billing_automation.sql
autonomous: true
requirements: [SEC-02, SEC-04, SEC-06]
must_haves:
  truths:
    - "D-07: D-08: An automation identity can perform only a caller-bound named command for a named account/provider/policy/action within its active limit."
    - "Automation receives no human multi-role exception and has no generic direct table authority."
    - "Grant validation, consumption, protected effect, and audit append are one transaction under replay and concurrency."
  artifacts:
    - path: supabase/migrations/20260901000003_billing_automation_grants.sql
      provides: "Automation principal/grant model and transactional effect-level authorization kernel"
    - path: supabase/tests/database/35_billing_automation.sql
      provides: "Grant mismatch, expiry, limit, replay, and concurrency contracts"
  key_links:
    - from: public.billing_automation_principals
      to: public.billing_automation_grants
      via: "verified auth_user_id and organization scope"
    - from: private.billing_consume_automation_grant
      to: public.billing_audit_events
      via: "row lock, bounded consumption, protected effect, and audit in one transaction"
---

<objective>
Create narrowly scoped, transactional authorization for automation callers.

Purpose: Service-role execution must never become a confused-deputy identity or inherit the owner's accepted role overlap.
Output: Principal/grant schema, effect-level kernel, and live database contracts.
</objective>

<decision_coverage>
- D-07: First-class automation principals receive exact named, bounded grants.
- D-08: Effect-level functions rebind caller/tenant and lock search path/ACLs.
</decision_coverage>

<execution_context>
@.planning/phases/02-tenant-role-and-evidence-security/02-CONTEXT.md
@.planning/phases/02-tenant-role-and-evidence-security/02-RESEARCH.md
@.planning/phases/02-tenant-role-and-evidence-security/02-PATTERNS.md
@supabase/tests/support/replay-concurrency.sql
@supabase/migrations/20260825000001_harden_lead_conversion.sql
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install first-class automation principals and exact grants</name>
  <files>supabase/migrations/20260901000003_billing_automation_grants.sql</files>
  <read_first>
    - Plan 01 migration helpers and `supabase/migrations/20260825000001_harden_lead_conversion.sql`
    - `supabase/tests/support/replay-concurrency.sql` for transactional claim semantics
    - `02-CONTEXT.md` D-07 through D-09 and D-15
  </read_first>
  <behavior>
    - Principal identity binds one Auth user to one active organization.
    - Grants name account, command, provider reference, policy version, action kind, validity, and optional amount/action limits.
    - Missing, expired, disabled, mismatched, or exhausted grants fail without a protected effect.
  </behavior>
  <action>Implement D-07, D-08, D-09, and D-15 as separate automation principal/grant tables with force RLS and no direct automation table policy. Add a private effect-level consumption helper that re-resolves `auth.uid()`, locks the matching grant/counter, validates every tuple element and bound, performs only a callback-specific protected operation, and appends an allowlisted audit result atomically. Use exact numerics without JavaScript, empty search_path, schema qualification, and explicit ACLs. Do not implement live provider commands or money movement. [BLOCKING] Prove installation only through the isolated schema-push target.</action>
  <acceptance_criteria>
    - Principal/grant constraints include every exact tuple and status/validity/limit field from D-07.
    - Automation has no direct generic protected-table policy and cannot inherit human role assignment.
    - The isolated schema-push command exits 0 and catalog ACLs expose only the intended effect entry point.
  </acceptance_criteria>
  <verify><automated>make test-financial-schema-push</automated></verify>
  <done>Automation authority is explicit, exact, bounded, server-derived, and unavailable through generic table access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove fail-closed tuple matching, consumption, replay, and locking</name>
  <files>supabase/tests/database/35_billing_automation.sql</files>
  <read_first>
    - `supabase/tests/support/replay-concurrency.sql`
    - Plan 04 Task 1 migration and `supabase/tests/database/20_rpc_trigger.sql`
  </read_first>
  <behavior>
    - Exact grants allow one synthetic nonfinancial test effect and one audit record.
    - Every tuple mismatch, expiry, disabled state, and limit breach leaves effect/counter/audit state exact.
    - Simultaneous consumption cannot exceed the action limit or duplicate one command key.
  </behavior>
  <action>Build pgTAP cases using the Phase 1 transactional claim/locking pattern and D-16 fixtures. Exercise two organizations, wrong account/provider/policy/action, missing grant, expired/disabled principal, amount/action limits, retryable failure, duplicate key, and concurrent last-unit consumption. Verify helpers' catalog security and no authenticated direct mutations of grants/counters/audit. Use synthetic commands only.</action>
  <acceptance_criteria>
    - Every tuple mismatch/status/expiry/limit/replay case has an exact unchanged-effect assertion.
    - Concurrent last-unit cases cannot commit more than one allowed effect or counter increment.
    - `make test-financial-database-sql` exits 0 with no live provider or money command.
  </acceptance_criteria>
  <verify><automated>make test-financial-database-sql</automated></verify>
  <done>Live PostgreSQL proves automation cannot widen, replay, race, or self-assign authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Automation JWT → service-role executor | A valid machine session could trick elevated code into another account/action. |
| Concurrent commands → grant counters | Races could exceed limits or duplicate protected effects. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Spoofing / Elevation | automation grant kernel | mitigate | Auth binding plus exact tuple validation inside effect transaction. |
| T-02-09 | Tampering / Repudiation | grant consumption | mitigate | Row locks, command uniqueness, exact counters, append-only audit. |
| T-02-SC | Tampering | dependencies | mitigate | No package install; pinned PostgreSQL/Supabase stack. |
</threat_model>

<verification>
- `make test-financial-schema-push`
- `make test-financial-database-sql`
</verification>

<success_criteria>
- Automation is isolated from human roles and direct resource policies.
- Exact grants are consumed transactionally with protected effects.
- Replay, concurrency, mismatch, and bound failures are executable.
</success_criteria>

<output>Create `.planning/phases/02-tenant-role-and-evidence-security/02-04-SUMMARY.md` when done.</output>
