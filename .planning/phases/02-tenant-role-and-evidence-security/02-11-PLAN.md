---
phase: 02-tenant-role-and-evidence-security
plan: "11"
type: execute
wave: 5
depends_on: ["02-10"]
files_modified:
  - src/components/atomic-crm/root/CRM.tsx
  - src/components/atomic-crm/billing-accounts/BillingAccountList.tsx
  - qa/billing-accounts.surface.source.json
  - qa/billing-accounts.surface.preview.json
  - qa/billing-accounts.surface.production.json
  - scripts/release/run-billing-source-surface.mjs
  - tests/release/billing-security-static.test.ts
autonomous: true
requirements: [WORK-01, SEC-03, SEC-07]
must_haves:
  truths:
    - "D-17: Billing accounts are reachable in both desktop and mobile application trees and are governed by staged rendered receipts."
    - "The rendered source surface reflows at 320px and a wider viewport without overlays, occluded focus, horizontal scroll, or unsafe visible content."
    - "Preview and production contracts require the exact canonical route and same deployment freshness marker before readiness/live claims."
  artifacts:
    - path: src/components/atomic-crm/root/CRM.tsx
      provides: "Desktop/mobile billing resource registration"
    - path: qa/billing-accounts.surface.source.json
      provides: "Two-viewport source rendered contract"
    - path: qa/billing-accounts.surface.preview.json
      provides: "Five-viewport immutable preview contract"
    - path: qa/billing-accounts.surface.production.json
      provides: "Independent canonical production contract"
    - path: scripts/release/run-billing-source-surface.mjs
      provides: "Bounded local source server and surface-gate orchestration"
  key_links:
    - from: CRM desktop/mobile Admin trees
      to: billingAccounts resource
      via: "Resource registration with responsive list/create/edit/show"
    - from: surface contracts
      to: BillingAccountList freshness marker
      via: "exact route/content marker and viewport matrix"
---

<objective>
Make the billing-account resource reachable on both surfaces and encode the full rendered release contract.

Purpose: Source diffs and HTTP status do not prove responsive, canonical, deployment-fresh UI.
Output: Resource registration, freshness marker, static wiring checks, and source/preview/production gate contracts.
</objective>

<decision_coverage>
- D-17: Source, five-viewport immutable preview, and independent production receipts are encoded with residual manual coverage named.
</decision_coverage>

<execution_context>
@.planning/phases/02-tenant-role-and-evidence-security/02-UI-SPEC.md
@.planning/phases/02-tenant-role-and-evidence-security/02-CONTEXT.md
@.planning/phases/02-tenant-role-and-evidence-security/02-PATTERNS.md
@src/components/atomic-crm/root/CRM.tsx
@/Users/agenticmac/.agents/skills/build-gate/references/surface-loop.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register billing accounts in desktop and mobile Admin trees</name>
  <files>src/components/atomic-crm/root/CRM.tsx, src/components/atomic-crm/billing-accounts/BillingAccountList.tsx, tests/release/billing-security-static.test.ts</files>
  <read_first>
    - `root/CRM.tsx` complete desktop/mobile Resource sets and imports
    - Billing resource `index.tsx` and responsive list from Plan 09
    - `02-UI-SPEC.md` navigation/presentation and D-14/D-17 marker needs
  </read_first>
  <behavior>
    - Desktop and mobile register the same `billing_accounts` resource and appropriate responsive components.
    - Navigation label/icon/action availability follows presentation capabilities without affecting provider authority.
    - The rendered route exposes one non-sensitive versioned freshness marker used by every surface stage.
  </behavior>
  <action>Add failing registration/marker source contracts, then wire the resource into both Admin trees following the CRM analog and D-14. Use the existing resource/nav primitives and one explicit non-secret marker such as `billing-security-phase2`; do not include commit SHA, tenant IDs, contacts, or secrets in visible content. Ensure direct route navigation still relies on provider authorization, not sidebar visibility.</action>
  <acceptance_criteria>
    - Static test finds exactly one `billing_accounts` Resource in each desktop/mobile Admin tree with intended components.
    - Direct route remains registered regardless of nav visibility and provider/RLS remains its data authority.
    - Marker `billing-security-phase2` renders on the account surface and contains no runtime/customer identity.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/billing-security-static.test.ts -t registration</automated></verify>
  <done>Desktop/mobile users can reach the responsive resource, and deployment freshness has a safe deterministic marker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode source, immutable preview, and production surface contracts</name>
  <files>qa/billing-accounts.surface.source.json, qa/billing-accounts.surface.preview.json, qa/billing-accounts.surface.production.json, scripts/release/run-billing-source-surface.mjs, tests/release/billing-security-static.test.ts</files>
  <read_first>
    - `/Users/agenticmac/.agents/skills/build-gate/references/surface-loop.md`
    - Build-gate example contracts referenced by that document
    - Task 1 route and freshness marker
  </read_first>
  <behavior>
    - Source contract renders the account route at 320px and one wider viewport before PR readiness.
    - Preview contract renders the immutable deployed preview at all five required viewports and requires the intended marker/canonical identity.
    - Production contract is independent, canonical, five-viewport, and requires the same marker after release.
  </behavior>
  <action>Translate D-17 and the build-gate surface-loop reference into closed JSON contracts with exact route, viewport dimensions, canonical/final URL expectations, freshness selector/text, mobile reflow, touch targets, fixed/overlay/focus/browser-error checks, and screenshot/receipt destinations. Add a dependency-free Node runner with `--self-test` and `run` modes that starts the deterministic demo source server on a loopback port, waits for readiness, invokes the shared Python gate with argv (never a shell string), forwards receipt/screenshots/stage arguments, redacts output, and always terminates the child. Static tests must reject missing stages/viewports/marker/canonical rules and any contract containing credentials or tenant data. Missing Playwright/browser is a blocking runtime failure, not a skipped pass.</action>
  <acceptance_criteria>
    - Source contract has exactly 320px and one wider viewport; preview/production each have the shared five-viewport matrix.
    - Every contract requires the exact billing route, canonical/final identity, `billing-security-phase2`, and all surface safety assertions.
    - Static contract tests and `node scripts/release/run-billing-source-surface.mjs --self-test` exit 0 without secrets or tenant fixtures.
  </acceptance_criteria>
  <verify><automated>npm test -- --run tests/release/billing-security-static.test.ts -t surface &amp;&amp; node scripts/release/run-billing-source-surface.mjs --self-test</automated></verify>
  <done>All three rendered gates are deterministic, fail-closed, and ready for their correct source/preview/production moments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Route/navigation → protected resource | Hidden UI could be mistaken for authorization. |
| Source/preview/production URL → readiness claim | Stale or wrong-origin content could masquerade as the intended release. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-26 | Elevation | resource navigation | mitigate | Direct route still crosses live provider/RLS; source contract checks both trees only. |
| T-02-27 | Spoofing / Tampering | surface receipts | mitigate | Exact canonical/final URL, freshness marker, immutable preview, independent production receipt. |
| T-02-SC | Tampering | gate runtime | mitigate | Repository/shared gate script only; runtime absence fails closed. |
</threat_model>

<verification>
- `npm test -- --run tests/release/billing-security-static.test.ts -t 'registration|surface'`
- Execute a `source` receipt before PR readiness per the contract.
</verification>

<success_criteria>
- Billing accounts are reachable on desktop and mobile.
- Source/preview/production contracts cannot be substituted or freshness-blind.
- Rendered proof remains separate at each release stage.
</success_criteria>

<output>Create `.planning/phases/02-tenant-role-and-evidence-security/02-11-SUMMARY.md` when done.</output>
