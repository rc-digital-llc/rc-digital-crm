---
phase: 01-executable-financial-test-and-release-gate
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - .gitignore
  - .gitleaks.toml
  - .env.development
  - supabase/functions/.env
  - scripts/release/security-gate.mjs
  - tests/release/security-gate.test.ts
autonomous: true
requirements:
  - REL-05
must_haves:
  truths:
    - "A release owner sees a blocking failure for every unresolved critical/high production dependency advisory."
    - "Git history and the current tree are scanned for secrets with values fully redacted."
    - "Tracked environment files cannot carry live secrets into future commits."
    - "A production frontend build contains no public source-map file."
  artifacts:
    - path: "package-lock.json"
      provides: "Reviewed dependency graph with zero critical/high production audit findings"
    - path: ".gitleaks.toml"
      provides: "Versioned secret-scan configuration limited to demonstrably synthetic allowlisting"
    - path: "scripts/release/security-gate.mjs"
      provides: "Combined dependency, secret, source-map, and coupling gate"
    - path: "tests/release/security-gate.test.ts"
      provides: "Negative recurrence tests for every REL-05 class"
  key_links:
    - from: "scripts/release/security-gate.mjs"
      to: "package-lock.json"
      via: "npm audit --omit=dev --audit-level=high"
      pattern: "npm audit"
    - from: "scripts/release/security-gate.mjs"
      to: "dist/"
      via: "post-build recursive `.map`/secret marker scan"
      pattern: "\\.map"
---

<objective>
Remediate the repository's current critical/high dependency, tracked-secret,
and public-source-map blockers, then make all four security classes fail closed.

Purpose: Enabling non-overridable gates before current blockers are repaired
would leave the repository permanently red or invite unsafe exceptions.
Output: Reviewed lockfile updates, secret hygiene/scanning, source-map removal,
and a reproducible release-security command with negative tests.
</objective>

<execution_context>
@/Users/agenticmac/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agenticmac/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-CONTEXT.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-RESEARCH.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-PATTERNS.md
@package.json
@package-lock.json
@vite.config.ts
@.gitignore
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear critical/high production dependency advisories</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - `package.json` and `package-lock.json` for direct/transitive dependency ownership
    - `.planning/phases/01-executable-financial-test-and-release-gate/01-RESEARCH.md` Current Risk Inventory and npm audit source
    - `src/components/atomic-crm/root/CRM.tsx` for React Router/ra-core integration risk
    - `src/providers/posthog.ts` for PostHog/protobuf transitive risk
    - `vite.config.ts` for build/PWA compatibility risk
  </read_first>
  <action>
    Re-run the redacted production audit as source of truth. Update direct
    vulnerable ranges to the verified compatible lines `lodash` 4.18.1,
    `react-router` 7.18.2, and `vite` 7.3.6 or newer non-breaking patch within
    those major lines. Update `posthog-js` within major 1 and the owning direct
    dependencies needed to eliminate transitive `protobufjs`, `nanoid`,
    `picomatch`, `postcss`, `react-router-dom`, and `ws` critical/high findings;
    prefer upstream fixed releases over `overrides`. Use `npm install` to
    regenerate lockfile integrity, review every major change rather than run
    `npm audit fix --force`, and run router/auth/Markdown/analytics/PWA
    regressions plus typecheck/build. Do not accept, omit, or exception any
    remaining critical/high production advisory per D-13.
  </action>
  <acceptance_criteria>
    - `npm audit --omit=dev --audit-level=high` exits 0 with critical=0 and high=0.
    - Lockfile resolves fixed versions for every named current critical/high package or removes the vulnerable path.
    - `npm ci`, full Vitest, typecheck, and production build all exit 0.
    - No `--force`, audit ignore, severity downgrade, or package-registry substitution is committed.
  </acceptance_criteria>
  <verify>
    <automated>npm ci &amp;&amp; npm audit --omit=dev --audit-level=high &amp;&amp; npm test -- --run &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>The reproducible production dependency graph contains no unresolved critical/high advisory and passes application regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove tracked secret-bearing files and add redacted history gates</name>
  <files>.gitignore, .gitleaks.toml, .env.development, supabase/functions/.env, scripts/release/security-gate.mjs, tests/release/security-gate.test.ts</files>
  <read_first>
    - `.gitignore` and `.env.example` for repository environment-file conventions
    - `git ls-files .env.development supabase/functions/.env .env.example` output noted in RESEARCH.md; do not open secret-bearing files directly
    - `.planning/phases/01-executable-financial-test-and-release-gate/01-RESEARCH.md` secret/privacy findings and Gitleaks 8.30.1 commands
    - `.planning/phases/01-executable-financial-test-and-release-gate/01-PATTERNS.md` redaction/fail-closed guidance
  </read_first>
  <behavior>
    - Current-tree and full-history scans run with `--redact=100` and fail on a finding without printing its value.
    - `.env.development` and `supabase/functions/.env` are untracked/ignored; `.env.example` remains sanitized and tracked.
    - Allowlisting requires exact synthetic fixture path plus rule/fingerprint rationale; broad regex/path exclusions fail tests.
    - A confirmed live credential produces a rotation-required identifier and keeps the gate red until owner rotation evidence is recorded privately.
  </behavior>
  <action>
    Without reading or echoing values, run Gitleaks 8.30.1 in fully redacted
    current-tree and full-history modes. Untrack/delete the two secret-bearing
    environment files from Git while adding exact ignore rules; retain only a
    sanitized `.env.example`. Create a narrow `.gitleaks.toml` that may allow
    only fixed synthetic test fixtures with rule ID and justification—never
    entire `.env`, migration, function, or history paths. Add `security-gate.mjs
    secrets` to execute both scans and emit rule/file/commit identifiers plus a
    report SHA-256, never values. If confirmed live credentials are found, keep
    the task/gate blocked until the owner rotates them through the provider and
    a private rotation receipt references the finding hash; do not place that
    receipt or credential metadata in this public repository.
  </action>
  <acceptance_criteria>
    - Git no longer tracks `.env.development` or `supabase/functions/.env`, and `.gitignore` names them exactly.
    - Both `gitleaks git --redact=100` and current-tree scan are enforced by `test-release-secrets`.
    - Test fixtures prove secret values never appear in stdout/stderr/report summaries.
    - Broad allowlists and a synthetic committed secret each make the targeted test fail.
    - Any real finding remains blocking until rotation evidence exists; there is no waiver path.
  </acceptance_criteria>
  <verify>
    <automated>make test-release-secrets</automated>
  </verify>
  <done>Tracked secret-bearing files are removed, history/current scans are fully redacted, and confirmed exposure cannot be waived.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove public source maps and complete the release-security gate</name>
  <files>vite.config.ts, scripts/release/security-gate.mjs, tests/release/security-gate.test.ts</files>
  <read_first>
    - `vite.config.ts` for the current `build.sourcemap: true` setting and build structure
    - `.github/workflows/deploy.yml` for current coupled production mutation markers
    - `scripts/release/security-gate.mjs` and test file from Task 2
    - `.github/release/release-policy.json` for non-overridable security classes
    - `.planning/phases/01-executable-financial-test-and-release-gate/01-PATTERNS.md` source-map pattern
  </read_first>
  <behavior>
    - Production build produces no `.map` file anywhere in the publish tree.
    - Bundle scan rejects service-role/access-token/private-key markers without printing matched values.
    - Gate rejects a workflow that combines `db push`, `functions deploy`, frontend build, and publish under automatic `main` push.
    - Dependency, secret, source-map, and coupling sub-gates remain separately reported and non-overridable.
  </behavior>
  <action>
    Set production Vite source maps to `false`; if private observability upload
    is later added, it must delete maps before the publish artifact is hashed.
    Extend `security-gate.mjs` with `dependencies`, `secrets`, `bundle`,
    `coupling`, and `all` modes. `bundle` runs the real production build then
    recursively rejects `.map` files and secret markers with redacted paths;
    `coupling` parses workflow files and rejects any automatic `main` path that
    can perform database push, function deploy, frontend build, and publish in
    one release authority. Add negative fixture tests for each class. D-13
    forbids ignore flags or exceptions for all four sub-gates.
  </action>
  <acceptance_criteria>
    - `vite.config.ts` disables production source-map emission and built `dist` contains zero `.map` files.
    - Security tests inject a fake `.map`, secret marker, high advisory result, and coupled workflow; each fails only its named sub-gate.
    - Output includes sub-gate status/report SHA-256 but no secret/advisory exploit value.
    - `make test-release-security` exits 0 only when all four sub-gates pass.
  </acceptance_criteria>
  <verify>
    <automated>make test-release-bundle &amp;&amp; make test-release-security</automated>
  </verify>
  <done>The `financial / release-security` implementation blocks critical/high advisories, secrets, public maps, and unsafe coupling with no waiver path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry/advisories → lockfile/build | Compromised or vulnerable dependency code enters production. |
| Git history/files → scanner/logs | Credentials may already be exposed and must not be re-exposed by scanning. |
| Vite build → public artifact | Source maps or embedded secret markers become customer-visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | dependency graph | mitigate | Zero critical/high production audit, reviewed upstream upgrades, lock integrity, full regressions. |
| T-01-11 | Information Disclosure | Git secrets/reports | mitigate | Untrack env files, Gitleaks history/tree with full redaction, private rotation evidence. |
| T-01-12 | Information Disclosure | production bundle | mitigate | Disable source maps and scan final publish tree/secret markers. |
| T-01-SC | Tampering | package update | mitigate | Existing legitimate packages only, npm integrity, no force/ignore/registry substitution. |
</threat_model>

<verification>
- `npm audit --omit=dev --audit-level=high`
- `make test-release-secrets`
- `make test-release-bundle`
- `make test-release-security`
- `git ls-files` confirms secret-bearing env files are absent and `.env.example` remains sanitized.
</verification>

<success_criteria>
- Critical/high production audit count is zero.
- Full-history/current secret scans are redacted and blocking.
- Production artifacts contain no source maps or secret markers.
- Unsafe deploy coupling is a separate failing sub-gate.
</success_criteria>

<output>
Create `.planning/phases/01-executable-financial-test-and-release-gate/01-07-SUMMARY.md` when done.
</output>

