---
phase: 01-executable-financial-test-and-release-gate
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - supabase/functions/postmark/index.ts
  - supabase/tests/fixtures/functions.env
  - tests/release/fixtures/postmark-inbound.json
  - tests/release/fixtures/provider-contract.json
  - tests/release/edge-webhook-provider.test.ts
autonomous: true
requirements:
  - REL-02
  - REL-03
must_haves:
  truths:
    - "A maintainer can exercise running Edge Functions and the current Postmark provider boundary under authenticated and rejected requests."
    - "A downstream Postmark database failure cannot be swallowed and acknowledged as 200 success."
    - "Synthetic provider fixtures cover method, authentication, body, failure, and success contracts without customer data or credentials."
    - "Future financial provider paths cannot satisfy the provider lane with source-string or happy-path-only tests."
  artifacts:
    - path: "tests/release/edge-webhook-provider.test.ts"
      provides: "Running Edge/webhook/provider success and failure contracts"
    - path: "tests/release/fixtures/provider-contract.json"
      provides: "Required provider test-case and path-ownership registration"
    - path: "supabase/functions/postmark/index.ts"
      provides: "Non-swallowing current provider failure behavior"
  key_links:
    - from: "tests/release/edge-webhook-provider.test.ts"
      to: "http://127.0.0.1:54321/functions/v1/postmark"
      via: "HTTP requests to served Edge Runtime"
      pattern: "functions/v1/postmark"
    - from: "supabase/functions/postmark/index.ts"
      to: "addNoteToContact"
      via: "propagates returned error Response before 200 acknowledgement"
      pattern: "await addNoteToContact"
---

<objective>
Create executable Edge Function, webhook, and current external-provider
contracts, and correct the known swallowed Postmark failure.

Purpose: A financial release gate must prove the actual Edge Runtime and
acknowledgement boundary, not TypeScript imports or SQL strings.
Output: Synthetic provider fixtures, running HTTP integration tests, and
fail-closed Postmark downstream-error propagation.
</objective>

<execution_context>
@/Users/agenticmac/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agenticmac/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-CONTEXT.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-RESEARCH.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-PATTERNS.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-02-SUMMARY.md
@supabase/functions/postmark/index.ts
@supabase/functions/postmark/addNoteToContact.ts
@supabase/functions/postmark/getExpectedAuthorization.ts
@supabase/functions/_shared/authentication.ts
@supabase/functions/_shared/utils.ts
@supabase/config.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define synthetic Edge and provider contract fixtures</name>
  <files>supabase/tests/fixtures/functions.env, tests/release/fixtures/postmark-inbound.json, tests/release/fixtures/provider-contract.json, tests/release/edge-webhook-provider.test.ts</files>
  <read_first>
    - `supabase/functions/postmark/index.ts` lines 26-214 for request checks and sample payload shape
    - `supabase/functions/postmark/getExpectedAuthorization.ts` for Basic authentication encoding
    - `supabase/functions/_shared/authentication.ts` for bearer-token Edge behavior
    - `.github/release/financial-paths.json` and `release-policy.json` for provider path ownership/non-overridable class
    - `.planning/phases/01-executable-financial-test-and-release-gate/01-PATTERNS.md` webhook fixture and HTTP patterns
  </read_first>
  <behavior>
    - Fixture validation rejects a real-looking domain, non-synthetic credential, missing provider event ID, or missing failure/replay/concurrency case registration.
    - The Postmark fixture contains reserved-example identities and deterministic content only.
    - Contract registration maps each provider path to auth, malformed-body, downstream-failure, duplicate/replay, concurrency, and success test names.
  </behavior>
  <action>
    Extract the inline Postmark payload into deterministic JSON using only
    `example.com`, fixed IDs, and synthetic content. Add a synthetic function
    env file containing test-only Postmark Basic values and loopback IPs; never
    copy `supabase/functions/.env`. Define `provider-contract.json` with a
    version, provider/path, financial boolean, event identity field, required
    case classes, and test file mapping. Start
    `edge-webhook-provider.test.ts` with fast fixture/contract validation so any
    future path classified financial must register every required case; keep
    current Postmark marked non-financial while still testing its live boundary.
  </action>
  <acceptance_criteria>
    - All fixture domains are IANA/reserved examples and all IDs/timestamps are fixed.
    - No tracked fixture contains production URL, real email, service role, access token, or secret pattern.
    - Provider contract requires auth, body, failure, replay, concurrency, and success case classes for financial paths.
    - Targeted fixture tests exit 0 and negative non-synthetic/missing-case variants fail.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/release/edge-webhook-provider.test.ts -t fixtures</automated>
  </verify>
  <done>Edge/provider inputs and required case ownership are deterministic, synthetic, and enforceable for current and future financial paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise running Edge contracts and propagate Postmark failures</name>
  <files>supabase/functions/postmark/index.ts, tests/release/edge-webhook-provider.test.ts</files>
  <read_first>
    - `supabase/functions/postmark/index.ts` for current check order and unconditional 200 response
    - `supabase/functions/postmark/addNoteToContact.ts` for returned 403/500 Response values
    - `supabase/functions/_shared/utils.ts` for JSON error response convention
    - `supabase/config.toml` for `verify_jwt = false` and manual function auth boundaries
    - `tests/release/fixtures/postmark-inbound.json` and `provider-contract.json` from Task 1
  </read_first>
  <behavior>
    - Running Postmark returns 401 for missing/bad forwarded IP or Basic auth, 405 for non-POST, and 403 for malformed required fields.
    - A downstream `addNoteToContact` 403/500 is returned and the handler does not acknowledge 200.
    - A fully seeded synthetic success returns 200 and exact expected note/contact effects.
    - Authenticated functions reject missing/invalid Bearer JWTs under local Edge Runtime.
  </behavior>
  <action>
    Start functions through the Plan 01 lane runner with the synthetic env file
    and test actual `functions/v1/*` URLs using Vitest/Node fetch. Add explicit
    request/response/effect assertions for method, IP, Basic auth, body,
    downstream database failure, and success. Change the Postmark loop to
    capture the `Response | undefined` from `addNoteToContact`; immediately
    return any Response and emit 200 only when every contact operation succeeds.
    Do not claim Postmark idempotency: its duplicate limitation is recorded as
    non-financial, while the release policy requires replay/concurrency coverage
    before any financial provider path can register. Do not expose Basic values,
    JWTs, request bodies, or internal database errors in logs.
  </action>
  <acceptance_criteria>
    - Live local HTTP tests cover 401, 403, 405, 500, and 200 behavior with database effect assertions.
    - Returned `addNoteToContact` error responses prevent final 200 acknowledgement.
    - Function tests run against the isolated local Edge Runtime with synthetic env only.
    - No test counts source imports, a direct handler call, or log text as runtime proof.
    - The `financial / edge-provider-contracts` command exits 0.
  </acceptance_criteria>
  <verify>
    <automated>make test-financial-functions</automated>
  </verify>
  <done>The running Edge/provider lane proves authenticated failure/success behavior, and current Postmark downstream failures are no longer swallowed as success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External provider → Postmark Edge endpoint | IP, Basic auth, method, and JSON body are untrusted inputs. |
| Edge handler → service-role database calls | A validated request crosses into privileged cross-table effects. |
| Database helper → webhook acknowledgement | Returned errors must not be ignored before 200 response. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Spoofing | Postmark/Edge auth | mitigate | Live invalid/missing IP, Basic, Bearer, and method assertions. |
| T-01-08 | Repudiation / Tampering | webhook acknowledgement | mitigate | Propagate downstream Response and assert database effects before 200. |
| T-01-11 | Information Disclosure | fixtures/logs | mitigate | Reserved synthetic data, test-only env, redacted assertions/logs. |
| T-01-SC | Tampering | Edge test stack | mitigate | Existing Vitest/fetch and pinned Supabase CLI; no package added. |
</threat_model>

<verification>
- `npm test -- --run tests/release/edge-webhook-provider.test.ts -t fixtures`
- `make test-financial-functions`
- Confirm every case reaches a running local function URL and inspects effects.
- Confirm no real customer/provider/credential value or swallowed error remains.
</verification>

<success_criteria>
- Edge and webhook contracts execute under the local runtime.
- Provider authentication, malformed input, downstream failure, and success are proven.
- Current Postmark no longer returns 200 after a returned downstream error.
- Future financial provider paths must register failure, replay, and concurrency ownership.
</success_criteria>

<output>
Create `.planning/phases/01-executable-financial-test-and-release-gate/01-05-SUMMARY.md` when done.
</output>

