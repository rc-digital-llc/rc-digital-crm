---
phase: 01-executable-financial-test-and-release-gate
plan: 06
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - supabase/tests/support/replay-concurrency.sql
  - tests/release/replay-concurrency.test.ts
autonomous: true
requirements:
  - REL-02
  - REL-03
must_haves:
  truths:
    - "A maintainer can prove duplicate, replayed, simultaneous, and reordered commands stop without duplicate financial effects."
    - "Concurrency proof runs against real PostgreSQL transactions, unique constraints, and locks rather than an in-memory mock."
    - "A financial provider or command path cannot register for release without naming its replay and concurrency tests."
  artifacts:
    - path: "supabase/tests/support/replay-concurrency.sql"
      provides: "Test-only idempotency, ordering, and claim/lock contract"
    - path: "tests/release/replay-concurrency.test.ts"
      provides: "Parallel process execution and exact effect assertions"
  key_links:
    - from: "tests/release/replay-concurrency.test.ts"
      to: "supabase/tests/support/replay-concurrency.sql"
      via: "parallel psql sessions inside the disposable Supabase DB container"
      pattern: "docker exec.*psql"
    - from: "tests/release/replay-concurrency.test.ts"
      to: "tests/release/fixtures/provider-contract.json"
      via: "registered replay/concurrency ownership verification"
      pattern: "provider-contract\\.json"
---

<objective>
Create a real PostgreSQL concurrency/replay test contract that future financial
commands and provider handlers must satisfy before merge or promotion.

Purpose: Sequential happy-path tests cannot prove idempotency, duplicate
suppression, ordering tolerance, or safe simultaneous execution.
Output: Test-only transactional support objects and a parallel Vitest lane with
exact effect-count assertions.
</objective>

<execution_context>
@/Users/agenticmac/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agenticmac/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-CONTEXT.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-RESEARCH.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-PATTERNS.md
@.planning/phases/01-executable-financial-test-and-release-gate/01-02-SUMMARY.md
@tests/release/fixtures/provider-contract.json
@supabase/migrations/20260306000008_attribution_triggers.sql
@supabase/config.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define test-only idempotency, ordering, and claim invariants</name>
  <files>supabase/tests/support/replay-concurrency.sql, tests/release/replay-concurrency.test.ts</files>
  <read_first>
    - `.planning/phases/01-executable-financial-test-and-release-gate/01-CONTEXT.md` for D-02, D-04, and D-13
    - `.planning/phases/01-executable-financial-test-and-release-gate/01-RESEARCH.md` provider/replay architecture
    - `supabase/migrations/20260306000008_attribution_triggers.sql` as the nearest transactional trigger analog and known race-prone pattern
    - `supabase/tests/support/auth-fixtures.sql` for test-schema lifecycle conventions
    - `tests/release/fixtures/provider-contract.json` for required case ownership
  </read_first>
  <behavior>
    - A first command/event key is claimed once and produces one effect.
    - Sequential duplicate, concurrent duplicate, and replay after completion produce no second effect.
    - An earlier sequence arriving after a later sequence is recorded/ignored according to the explicit order rule without regressing state.
    - A failed claimed transaction leaves no committed effect and is retryable under the same key only according to an explicit terminal/retryable state rule.
  </behavior>
  <action>
    Create only test-scoped SQL under `test_release`: an inbox/command table
    with a unique provider/event key, explicit sequence/state, an effect table,
    and a claim/apply function using one transaction, conflict handling, and
    row locking. Do not add this support schema to production migrations or
    imply it is the future financial ledger. In Vitest, first write sequential
    cases and provider-registration checks. Resolve exactly one local DB
    container for the repository project, then use explicit argv with `docker
    exec <container> psql ...` for database sessions because host `psql` is not
    available; reject zero/multiple matches and never use command strings.
  </action>
  <acceptance_criteria>
    - Test support SQL creates unique key, sequence/state, transactional claim, and exact effect-count invariants only under `test_release`.
    - Sequential duplicate/replay/reordered/failure cases each assert returned state and database effects.
    - Provider contract validation rejects any financial path lacking named replay and concurrency cases.
    - Cleanup removes the test schema and no production migration references it.
  </acceptance_criteria>
  <verify>
    <automated>make test-financial-concurrency-fixture</automated>
  </verify>
  <done>The disposable database exposes a deterministic contract capable of detecting duplicate, replay, ordering, and failed-claim defects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove simultaneous execution and replay safety</name>
  <files>tests/release/replay-concurrency.test.ts</files>
  <read_first>
    - `tests/release/replay-concurrency.test.ts` from Task 1
    - `supabase/tests/support/replay-concurrency.sql` from Task 1
    - `scripts/release/run-supabase-lane.mjs` for stack/container identity and cleanup
    - `.github/release/release-policy.json` for zero assertion retry and required replay lane identity
  </read_first>
  <behavior>
    - 32 simultaneous claims for one key yield exactly one winner, one durable effect, and 31 duplicate/non-owner results.
    - Parallel distinct keys all complete exactly once without cross-key blocking artifacts.
    - Replaying the completed key after process restart still produces one total effect.
    - The test fails on any duplicate effect, missing result, deadlock/timeout, swallowed subprocess error, or retry of an assertion.
  </behavior>
  <action>
    Extend the Vitest suite to launch fixed-size parallel psql argv processes
    against the test function, enforce per-process and suite timeouts, collect
    structured JSON rows, and assert exact winner/duplicate/effect counts. Run
    one same-key burst, one distinct-key burst, one reversed-sequence burst,
    and one post-process replay. Capture PostgreSQL errors with full secret
    redaction. Do not retry a failed burst, randomize counts/keys, or treat an
    expected lock timeout as success. The lane wrapper may retry only stack
    bootstrap before this test starts per D-04.
  </action>
  <acceptance_criteria>
    - Same-key burst returns exactly one winner and one committed effect.
    - Distinct-key burst returns one effect per key with no missing/duplicate effect.
    - Reordered and post-restart replay cases preserve the explicit state/effect invariants.
    - Every subprocess is bounded, exit-checked, and represented in the final assertion count.
    - `make test-financial-concurrency` exits 0 once and contains no assertion retry.
  </acceptance_criteria>
  <verify>
    <automated>make test-financial-concurrency</automated>
  </verify>
  <done>The `financial / replay-concurrency` lane provides deterministic real-transaction proof that duplicate and simultaneous operations stop safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent processes → PostgreSQL claim function | Duplicate/unordered commands race at the authoritative transaction boundary. |
| Docker container discovery → psql execution | Incorrect container selection could test or mutate unrelated state. |
| Subprocess results → test verdict | Missing/failed processes must not be counted as duplicate-safe success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering / Repudiation | replay/concurrency fixture | mitigate | Unique keys, transactional claim, exact counts, restart replay, no assertion retry. |
| T-01-03 | Tampering | container selection | mitigate | Require one exact test-project DB container and explicit argv. |
| T-01-11 | Information Disclosure | subprocess output | mitigate | Structured redaction and no connection credentials in failure output. |
| T-01-SC | Tampering | test tooling | mitigate | Existing Docker/Postgres image and Vitest only; no new package. |
</threat_model>

<verification>
- `make test-financial-concurrency-fixture`
- `make test-financial-concurrency`
- Confirm one-winner/one-effect assertions for 32 same-key processes.
- Confirm no test support object ships through `supabase/migrations/`.
</verification>

<success_criteria>
- Duplicate, replay, concurrency, order, and failed-claim behavior is proven in PostgreSQL.
- Every process result participates in a deterministic assertion.
- The lane has no assertion retry or shared remote authority.
- Future financial provider/command paths must register replay and concurrency coverage.
</success_criteria>

<output>
Create `.planning/phases/01-executable-financial-test-and-release-gate/01-06-SUMMARY.md` when done.
</output>

